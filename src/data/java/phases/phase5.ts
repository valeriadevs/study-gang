import type { Day } from '../../../types';

export const phase5days: Day[] = [
  // DAY 13: File I/O & Serialization
  {
    id: 'java-14-d13', number: 19, title: 'File Handling and Serialization', duration: 120,
    topics: ['FileInputStream', 'FileOutputStream', 'Serializable', 'Serialization / Deserialization'],
    blocks: [
      { type: 'callout', id: 'd13-intro', calloutType: 'info', title: 'Data That Outlives Your Program', content: 'Everything you have built so far lives only in RAM — all your variables, arrays, and objects disappear the moment your program ends. Today you will learn two ways to make data survive: **File I/O** reads and writes raw bytes or text to files on your disk, and **Serialization** saves entire Java objects (with all their fields) to a file and rebuilds them later. Think of File I/O as saving a text document, and Serialization as freezing an object and thawing it later.' },
      { type: 'heading', id: 'd13-streams', level: 2, content: 'File I/O — Reading and Writing Files' },
      { type: 'paragraph', id: 'd13-streams-p', content: 'Java has two families for file work: **Streams** (for raw bytes — images, audio, serialized objects) and **Readers/Writers** (for text — .txt, .csv, .json files). Today we focus on byte streams because they are the foundation. Every file operation follows the same pattern: open, read/write, close.' },
      { type: 'code', id: 'd13-stream-code', lang: 'java', title: 'Copying a File — The Simplest Pattern', code: '// The pattern: open, read/write, close (try-with-resources handles close)\ntry (FileInputStream fis = new FileInputStream("input.txt");\n     FileOutputStream fos = new FileOutputStream("output.txt")) {\n\n    int b;                           // read() returns one byte as an int\n    while ((b = fis.read()) != -1) { // -1 means "end of file" (no more bytes)\n        fos.write(b);                // write that byte to the output file\n    }\n    // When the try block ends, BOTH streams auto-close.\n\n} catch (IOException e) {\n    System.out.println("File error: " + e.getMessage());\n}' },
      { type: 'paragraph', id: 'd13-streams-explain', content: 'Let me walk through every line. `FileInputStream` opens a connection to read bytes from a file. `FileOutputStream` opens one to write bytes. The `while` loop reads one byte at a time — `fis.read()` returns the next byte (0-255) or `-1` when there are no more bytes left. Each byte we read gets written immediately to the output. The entire `try(...)` block is **try-with-resources** — Java automatically calls `close()` on both streams when the block ends, even if an error happens. No manual `finally` block needed.' },
      { type: 'callout', id: 'd13-auto-close', calloutType: 'tip', title: 'try-with-resources — The Modern Way', content: 'Any class that implements `AutoCloseable` (all I/O streams, Scanner, database connections) can go inside `try()`. Java guarantees `close()` is called when the block exits — even if an exception is thrown. This is cleaner and safer than the old pattern of `try { ... } finally { thing.close(); }`.' },
      { type: 'heading', id: 'd13-serial', level: 2, content: 'Serialization — Saving Entire Objects' },
      { type: 'paragraph', id: 'd13-serial-p', content: 'File I/O is great for text and bytes, but what if you want to save an entire object — say, a `Student` with name, roll number, and GPA? You could manually write each field to a file and read them back in the same order. But Java has a built-in shortcut: **Serialization**. Mark your class with `implements Serializable`, and Java handles converting the whole object to bytes and back automatically.' },
      { type: 'paragraph', id: 'd13-serial-steps', content: 'There are two steps. **Serializing** = object → bytes → file. You use `ObjectOutputStream` wrapped around a `FileOutputStream`. Call `writeObject(yourObject)` and Java handles the rest. **Deserializing** = file → bytes → object. You use `ObjectInputStream` wrapped around a `FileInputStream`. Call `readObject()` — it returns `Object`, so you must cast it back to your type.' },
      { type: 'code', id: 'd13-ser-code', lang: 'java', title: 'Complete Serialization Example', code: 'import java.io.*;\n\n// Step 1: Make your class serializable\nclass Student implements Serializable {\n    private static final long serialVersionUID = 1L; // version number\n    String name;\n    int rollNo;\n    transient String password;  // this field is NEVER saved to disk!\n\n    Student(String n, int r, String p) {\n        name = n; rollNo = r; password = p;\n    }\n}\n\npublic class SerializeDemo {\n    public static void main(String[] args) {\n        Student s1 = new Student("Vinayak", 101, "secret123");\n\n        // SERIALIZE: object → file\n        try (ObjectOutputStream oos = new ObjectOutputStream(\n                 new FileOutputStream("student.ser"))) {\n            oos.writeObject(s1);  // the whole object is saved in one call!\n            System.out.println("Student saved to student.ser");\n        } catch (IOException e) {\n            System.out.println("Save failed: " + e.getMessage());\n        }\n\n        // DESERIALIZE: file → object\n        try (ObjectInputStream ois = new ObjectInputStream(\n                 new FileInputStream("student.ser"))) {\n            Student loaded = (Student) ois.readObject();  // cast required!\n            System.out.println("Loaded: " + loaded.name + ", roll " + loaded.rollNo);\n            System.out.println("Password: " + loaded.password); // null! (transient)\n        } catch (IOException | ClassNotFoundException e) {\n            System.out.println("Load failed: " + e.getMessage());\n        }\n    }\n}' },
      { type: 'callout', id: 'd13-transient', calloutType: 'warn', title: 'transient — Protect Sensitive Data', content: 'Mark fields `transient` to exclude them from serialization. Use for: passwords, security tokens, computed/cached values, non-serializable object references. After deserialization, transient fields get **default values** (null, 0, false).' },
      // Doubt
      { type: 'callout', id: 'd13-d1', calloutType: 'doubt', title: 'FileInputStream vs FileReader — which to use?', content: '**FileInputStream** — raw bytes. Use for images, audio, serialized objects — anything non-text.\n\n**FileReader** — character-based. Use for text files — handles character encoding automatically.\n\nRule: bytes → Stream, text → Reader/Writer.' },
      { type: 'callout', id: 'd13-d2', calloutType: 'doubt', title: 'What if I serialize, then change the class (add/remove fields)?', content: 'You will get an **`InvalidClassException`** at deserialization time. The fix: declare `private static final long serialVersionUID = 1L;` in your class. This tells Java "this is the same class, even if fields changed." Without it, Java computes a hash from the class structure — any change breaks compatibility.' },
      // Exam
      { type: 'callout', id: 'd13-exam', calloutType: 'exam', title: 'Exam Alert', content: '1. **`Serializable` is a MARKER INTERFACE** — has NO methods. Tested in every CE-2.\n2. **`transient` keyword** — almost guaranteed coding or MCQ question.\n3. **`serialVersionUID`** — prevents version mismatch errors.\n4. **`ObjectInputStream.readObject()` returns `Object`** — must cast to the actual type.' },
      // Bridge
      { type: 'callout', id: 'd13-bridge', calloutType: 'bridge', title: 'Connect the Dots', content: 'File I/O + Exceptions (Day 18) + Classes (Day 9) combine into real-world data persistence. `Serializable` uses interfaces (Day 14). try-with-resources uses `AutoCloseable` — another interface.' },
      // Quick Ref
      { type: 'table', id: 'd13-qref', headers: ['Concept', 'Key Point'], rows: [['FileInputStream', 'Read raw bytes. read() returns -1 at EOF.'], ['FileOutputStream', 'Write raw bytes. Creates the file if it does not exist.'], ['Serializable', 'Marker interface. No methods. Required for ObjectOutputStream.'], ['transient', 'Excludes field from serialization. Defaults after deserialize.'], ['serialVersionUID', 'Class version identifier. Prevents InvalidClassException.']] },
      // Quiz
      { type: 'quiz', id: 'd13-quiz', title: 'Day 19 Quiz', questions: [
        { id: 'd13-q1', question: 'What happens to a transient field after deserialization?', options: ['It keeps its original value', 'It gets the default value (null, 0, false)', 'It throws an exception', 'It is automatically reconstructed'], correctIndex: 1, explanation: 'Transient fields are NOT serialized. After deserialization, they receive their default values: null for objects, 0 for numbers, false for boolean.' },
        { id: 'd13-q2', question: 'Why is Serializable called a marker interface?', options: ['It marks memory for GC', 'It has no methods — just marks the class', 'It is deprecated', 'It generates bytecode markers'], correctIndex: 1, explanation: 'A marker interface has no methods or constants. It simply "marks" a class as having a capability. Serializable tells the JVM "this class can be serialized."' },
        { id: 'd13-q3', question: 'Which method reads an object from a file?', options: ['ObjectOutputStream.writeObject()', 'ObjectInputStream.readObject()', 'FileInputStream.read()', 'Object.read()'], correctIndex: 1, explanation: 'readObject() on ObjectInputStream reads the next serialized object. It returns Object, so you must cast it to the actual type.' },
      ] },
      // Flashcards
      { type: 'flashcard', id: 'd13-cards', title: 'Day 19 Flashcards', cards: [
        { id: 'd13-f1', front: 'What is Serialization?', back: 'Converting an object into a byte stream for storage or transmission. Deserialization reconstructs the object from the byte stream. Class must implement Serializable.', hint: 'Object → bytes → file...' },
        { id: 'd13-f2', front: 'What does transient do?', back: 'Prevents a field from being serialized. The field gets its default value (null/0/false) after deserialization. Used for passwords, tokens, cached values.', hint: 'Exclude from serialization...' },
        { id: 'd13-f3', front: 'What is serialVersionUID?', back: 'A version identifier for serializable classes. Prevents InvalidClassException when class structure changes. Without it, JVM auto-generates a hash — any change breaks compatibility.', hint: 'Version number...' },
      ] },
      { type: 'practice', id: 'd13-p1', lang: 'java', title: 'Practice: Todo List Persistence', starter: 'import java.io.*;\nimport java.util.ArrayList;\nclass TodoItem implements Serializable {\n    private static final long serialVersionUID = 1L;\n    String task; boolean done;\n    TodoItem(String t) { task=t; done=false; }\n}\npublic class TodoList {\n    public static void main(String[] args) throws Exception {\n        ArrayList<TodoItem> list = new ArrayList<>();\n        list.add(new TodoItem("Study Java"));\n        // TODO: Save to file, load back, display\n    }\n}', hint: 'ObjectOutputStream to write. ObjectInputStream to read. Cast readObject() to ArrayList<TodoItem>. Handle FileNotFoundException for first run.' },
      { type: 'practice', id: 'd13-p2', lang: 'java', title: 'Practice: Log File Reader', starter: 'import java.io.*;\npublic class LogReader {\n    public static void main(String[] args) {\n        try (BufferedReader br = new BufferedReader(new FileReader("log.txt"))) {\n            String line; int count = 0;\n            while ((line = br.readLine()) != null) {\n                // TODO: count lines containing "ERROR"\n                count++;\n            }\n            System.out.println("Total lines: " + count);\n        } catch (IOException e) { System.out.println("File not found"); }\n    }\n}', hint: 'Use line.contains("ERROR") to count error lines. BufferedReader is more efficient for text — it reads chunks, not byte by byte.' },
      { type: 'practice', id: 'd13-p3', lang: 'java', title: 'Practice: Byte Copy with Progress', starter: 'import java.io.*;\npublic class CopyProgress {\n    public static void main(String[] args) throws IOException {\n        // TODO 1: open FileInputStream on "source.bin" and\n        //         FileOutputStream on "copy.bin" (try-with-resources)\n        // TODO 2: copy byte by byte like the lesson example\n        // TODO 3: count how many bytes were copied and print it\n        // TODO 4: EXTRA: read in chunks of 1024 bytes with a byte[]\n        //         buffer — read(byte[] b) returns bytes read, -1 at EOF\n    }\n}', hint: 'int b; while ((b = fis.read()) != -1) { fos.write(b); count++; }. For the buffer version: byte[] buf = new byte[1024]; int n; while ((n = fis.read(buf)) != -1) { fos.write(buf, 0, n); }' },
      { type: 'practice', id: 'd13-p4', lang: 'java', title: 'Practice: Student Database', starter: 'import java.io.*;\nimport java.util.ArrayList;\n\nclass Student implements Serializable {\n    private static final long serialVersionUID = 1L;\n    String name; int roll; double gpa;\n    Student(String n, int r, double g) { name=n; roll=r; gpa=g; }\n    public String toString() { return roll + " " + name + " " + gpa; }\n}\n\npublic class StudentDB {\n    public static void main(String[] args) throws Exception {\n        ArrayList<Student> students = new ArrayList<>();\n        students.add(new Student("Vinayak", 101, 8.5));\n        students.add(new Student("Riya", 102, 9.2));\n        students.add(new Student("Amit", 103, 7.8));\n\n        // TODO 1: save the whole list to "students.dat"\n        // TODO 2: clear the list, then load it back from the file\n        // TODO 3: print every student. Verify all three came back.\n        // TODO 4: what happens if you read the file again (no write)?\n    }\n}', hint: 'Save: ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("students.dat")); oos.writeObject(students);. Load: (ArrayList<Student>) new ObjectInputStream(new FileInputStream("students.dat")).readObject();' },
      { type: 'practice', id: 'd13-p5', lang: 'java', title: 'Practice: Write Lines with Writer', starter: 'import java.io.*;\npublic class WriteLines {\n    public static void main(String[] args) {\n        // TODO 1: write these lines to "notes.txt" using BufferedWriter\n        //         (use newLine() or write() with \\n)\n        //         "Java Day 19"\n        //         "File I/O is fun"\n        //         "One more line"\n        // TODO 2: append a 4th line WITHOUT overwriting the file\n        //         (hint: FileWriter second arg true = append)\n        // TODO 3: read the file back with BufferedReader and print it\n    }\n}', hint: 'try (BufferedWriter bw = new BufferedWriter(new FileWriter("notes.txt"))) { bw.write("Java Day 19"); bw.newLine(); ... }. Append: new FileWriter("notes.txt", true).' },
      { type: 'practice', id: 'd13-p6', lang: 'java', title: 'Practice: Transient Field Lab', starter: 'import java.io.*;\n\nclass Account implements Serializable {\n    private static final long serialVersionUID = 1L;\n    String username;\n    transient String password;   // should NEVER be saved\n    int balance;\n\n    Account(String u, String p, int b) { username=u; password=p; balance=b; }\n}\n\npublic class TransientLab {\n    public static void main(String[] args) throws Exception {\n        Account a = new Account("vinayak", "secret123", 5000);\n\n        // TODO 1: save `a` to "account.dat"\n        // TODO 2: load it back into a NEW Account variable\n        // TODO 3: print username, password, balance\n        //         what is password after loading? why?\n        // TODO 4: in a comment, explain why transient matters for security\n    }\n}', hint: 'password loads as null — transient fields are not serialized, so they get default values after deserialization. That is the whole point: never write secrets to disk.' },
    ],
    tasks: [
      { id: 'java-14-d13-t1', text: 'Copy a text file byte by byte using FileInputStream/FileOutputStream.', tag: 'lab' },
      { id: 'java-14-d13-t2', text: 'Serializable Book class (title, author, price). Save list to file, read back, display.', tag: 'lab' },
      { id: 'java-14-d13-t3', text: 'Add transient field to Book. Show it is null after deserialization.', tag: 'drill' },
    ],
  },

  // DAY 14: Collections & Sorting (Final Day)
  {
    id: 'java-14-d14', number: 20, title: 'The Collections Framework & Custom Sorting', duration: 120,
    topics: ['ArrayList', 'Comparable', 'compareTo', 'Comparator'],
    blocks: [
      { type: 'callout', id: 'd14-intro', calloutType: 'info', title: 'Collections — the everyday workhorse', content: 'Collections are the data structures you will use every day. Today: **ArrayList** (dynamic array), **Comparable** (natural ordering), and **Comparator** (custom ordering). Ties together interfaces, generics, and OOP. This closes out Phase 5 — next up is Phase 6 (Days 21-29): recursion, enums, generics, collections maps, lambdas, and streams.' },
      { type: 'heading', id: 'd14-overview', level: 2, content: 'Collections Framework at a Glance' },
      { type: 'table', id: 'd14-col-table', headers: ['Interface', 'Implementation', 'Ordering', 'Duplicates?', 'Use For'], rows: [['List', 'ArrayList, LinkedList', 'Insertion order', '✅ Yes', 'Indexed access'], ['Set', 'HashSet, TreeSet', 'Unordered / Sorted', '❌ No', 'Unique elements'], ['Map', 'HashMap, TreeMap', 'Unordered / Sorted', 'Keys: ❌  Values: ✅', 'Key-value pairs'], ['Queue', 'PriorityQueue', 'Priority / FIFO', '✅ Yes', 'Task scheduling']] },
      { type: 'heading', id: 'd14-arraylist', level: 2, content: 'ArrayList — The Workhorse' },
      { type: 'code', id: 'd14-al-code', lang: 'java', title: 'ArrayList Operations', code: "ArrayList<String> fruits = new ArrayList<>();\nfruits.add(\"Apple\");              // append\nfruits.add(1, \"Mango\");     // insert at index 1\nString first = fruits.get(0);        // access by index\nfruits.set(1, \"Kiwi\");        // update\nfruits.remove(\"Banana\");       // remove by value\nfruits.remove(0);              // remove by index\nint size = fruits.size();            // NOT length\nboolean has = fruits.contains(\"Kiwi\");\nint idx = fruits.indexOf(\"Orange\");\nCollections.sort(fruits);            // A-Z\nCollections.shuffle(fruits);         // randomize" },
      { type: 'heading', id: 'd14-comparable', level: 2, content: 'Comparable — Default Sorting' },
      { type: 'code', id: 'd14-comp-code', lang: 'java', title: 'Comparable: Sort by Roll No', code: 'class Student implements Comparable<Student> {\n    String name; int rollNo;\n    public int compareTo(Student other) {\n        return this.rollNo - other.rollNo;\n        // negative: this < other, zero: equal, positive: this > other\n    }\n}\nArrayList<Student> list = new ArrayList<>();\nCollections.sort(list); // uses compareTo() automatically' },
      { type: 'heading', id: 'd14-comparator', level: 2, content: 'Comparator — Alternative Sorting' },
      { type: 'code', id: 'd14-comp-code', lang: 'java', title: 'Comparator: Sort by Name, Then GPA', code: '// Sort by GPA descending (lambda)\nstudents.sort((s1, s2) -> Double.compare(s2.gpa, s1.gpa));\n\n// Chain: sort by name, then by roll number\nComparator<Student> c = Comparator\n    .comparing((Student s) -> s.name)\n    .thenComparingInt(s -> s.rollNo);\nstudents.sort(c);' },
      { type: 'table', id: 'd14-compare', headers: ['Aspect', 'Comparable', 'Comparator'], rows: [['Package', 'java.lang', 'java.util'], ['Method', 'compareTo(o)', 'compare(o1, o2)'], ['Sort orders', 'ONE (natural order)', 'Many (one per Comparator)'], ['Modifies class?', 'Yes', 'No (separate class or lambda)'], ['Usage', 'Collections.sort(list)', 'list.sort(comparator)'], ['When to use', 'One obvious sort order', 'Multiple sort criteria or cannot modify class']] },
      // Doubt
      { type: 'callout', id: 'd14-d1', calloutType: 'doubt', title: 'ArrayList vs LinkedList — which is faster?', content: '**ArrayList** — O(1) random access (get/set by index). O(n) insert/delete in the middle (shifts elements).\n\n**LinkedList** — O(n) random access (have to traverse the list). O(1) insert/delete at the ends.\n\n**Default**: use ArrayList. It is faster for almost everything. Only use LinkedList if you frequently insert or delete at the front.' },
      { type: 'callout', id: 'd14-d2', calloutType: 'doubt', title: "When compareTo returns 0, does equals() also return true?", content: '**NOT necessarily.** `compareTo()` returning 0 means "equal in ordering," but `equals()` may disagree. Example: two students with the same GPA but different names. `compareTo` (by GPA) returns 0. `equals` (by all fields) returns false.\n\nThis inconsistency is a known bug source. **Rule of thumb**: make `compareTo` consistent with `equals` when used in sorted sets/maps.' },
      // Exam
      { type: 'callout', id: 'd14-exam', calloutType: 'exam', title: 'Exam Alert', content: '1. **Comparable vs Comparator** — #1 Collections MCQ.\n2. **`compareTo()` return values**: negative / zero / positive. KNOW THIS.\n3. **`ArrayList.size()`** — NOT `.length` or `.length()` (third syntax).\n4. **Sorting custom objects** — appears in every coding round and End Sem.' },
      // Bridge
      { type: 'callout', id: 'd14-bridge', calloutType: 'bridge', title: 'Connect the Dots', content: 'ArrayList uses arrays (Day 8) internally. Sorting requires interfaces (Day 14). `Comparable.compareTo()` uses comparison operators (Day 3). Wrapper classes (Day 18) enable `ArrayList<Integer>` — generics need objects.' },
      // Quick Ref
      { type: 'table', id: 'd14-qref', headers: ['Concept', 'Key Point'], rows: [['ArrayList', 'Dynamic array. add(), get(), set(), remove(), size().'], ['Comparable', 'compareTo(o). Natural order. Implement in the class itself.'], ['Comparator', 'compare(a, b). Custom order. Lambda or separate class.'], ['compareTo returns', 'Negative (this < other), Zero (equal), Positive (this > other).'], ['Collections.sort()', 'Uses Comparable. Collections.sort(list, comp) uses Comparator.']] },
      // Quiz
      { type: 'quiz', id: 'd14-quiz', title: 'Day 20 Quiz', questions: [
        { id: 'd14-q1', question: 'What does compareTo() return when this object is less than the other?', options: ['0', 'A positive number', 'A negative number', 'true'], correctIndex: 2, explanation: 'Negative means "this comes before other." For numbers: this - other is negative when this < other. For strings: returns the difference in lexicographic order.' },
        { id: 'd14-q2', question: 'When should you use Comparator instead of Comparable?', options: ['When you need faster sorting', 'When you need multiple sort orders or cannot modify the class', 'When sorting primitive arrays', 'Comparator is always better'], correctIndex: 1, explanation: 'Comparator is external — you create it without modifying the class. Use it for alternative sort orders (by name, by GPA, by age) or when you cannot change the class (e.g., sorting String by length).' },
        { id: 'd14-q3', question: 'What is the correct way to get the size of an ArrayList?', options: ['list.length', 'list.length()', 'list.size()', 'list.count()'], correctIndex: 2, explanation: 'ArrayList uses .size(). Arrays use .length (field, no parens). Strings use .length() (method, with parens). Three different syntaxes for three different types!' },
      ] },
      // Flashcards
      { type: 'flashcard', id: 'd14-cards', title: 'Day 20 Flashcards', cards: [
        { id: 'd14-f1', front: 'ArrayList key methods?', back: 'add(E), add(index, E), get(index), set(index, E), remove(index), remove(Object), size(), contains(E), indexOf(E), clear(), isEmpty().', hint: 'CRUD operations...' },
        { id: 'd14-f2', front: 'Comparable vs Comparator?', back: 'Comparable: compareTo(o) in the class itself. Natural ordering. ONE sort order.\nComparator: compare(a,b) externally. Custom ordering. MANY sort orders possible without modifying class.', hint: 'Inside vs outside the class...' },
        { id: 'd14-f3', front: 'compareTo() return value meaning?', back: 'Negative = this comes BEFORE other. Zero = equal in ordering. Positive = this comes AFTER other. Convention: return this.field - other.field (for numbers).', hint: 'Negative, zero, positive...' },
      ] },
      // Course Complete
      { type: 'callout', id: 'd14-complete', calloutType: 'success', title: '🎉 Phase 5 Complete — halfway check-in', content: 'You have covered the **core university Java syllabus** — everything through collections and custom sorting.\n\n**Phase 1 (Days 1-5)**: Fundamentals — JDK/JVM, primitives, control flow, loops.\n**Phase 2 (Days 6-8)**: Methods, arrays, strings.\n**Phase 3 (Days 9-14)**: Full OOP — classes, constructors, inheritance, polymorphism, abstraction, interfaces.\n**Phase 4 (Days 15-18)**: Packages, access, 2D arrays, StringBuilder, exceptions, wrappers.\n**Phase 5 (Days 19-20)**: File I/O, serialization, collections, sorting.\n\n**Exam Preparation so far**:\n- CE-1: Focus on Days 1-8 (fundamentals + methods).\n- CE-2: Focus on Days 9-18 (OOP through wrappers).\n- End Sem: All 29 days. Prioritize Days 9-14 (OOP), 19-20 (collections/files), and 21-29 (modern Java).\n\nNext up — Phase 6 (Days 21-29): recursion, enums, generics, HashMap/TreeMap, lambdas, and streams. Keep practising. The best way to retain Java is to write Java — every single day.' },
      { type: 'practice', id: 'd14-p1', lang: 'java', title: 'Practice: Student Ranking System', starter: 'import java.util.*;\npublic class Ranking {\n    public static void main(String[] args) {\n        ArrayList<Student> list = new ArrayList<>();\n        list.add(new Student("Vinayak", 8.5));\n        list.add(new Student("Riya", 9.2));\n        list.add(new Student("Amit", 7.8));\n        list.add(new Student("Neha", 9.2));\n        // TODO: sort by GPA desc, then name asc for ties\n    }\n}\nclass Student implements Comparable<Student> {\n    String name; double gpa;\n    Student(String n, double g) { name=n; gpa=g; }\n    public int compareTo(Student o) { return Double.compare(o.gpa, this.gpa); } // desc\n    public String toString() { return name+" ("+gpa+")"; }\n}', hint: 'Collections.sort(list) uses Comparable (GPA desc). For ties, chain: list.sort(Comparator.comparingDouble(Student::getGpa).reversed().thenComparing(Student::getName)).' },
      { type: 'practice', id: 'd14-p2', lang: 'java', title: 'Practice: Library Catalog', starter: 'import java.util.*;\npublic class Library {\n    public static void main(String[] args) {\n        ArrayList<Book> books = new ArrayList<>();\n        books.add(new Book("Java Basics", "Oracle", 2020));\n        books.add(new Book("Data Structures", "Cormen", 2009));\n        books.add(new Book("Clean Code", "Martin", 2008));\n        // TODO: sort by year (oldest first), then by title alphabetically\n    }\n}\nclass Book { String title, author; int year; Book(String t, String a, int y) { title=t; author=a; year=y; } }', hint: 'Use Comparator chain: Comparator.comparingInt(Book::getYear).thenComparing(Book::getTitle). Add getters to Book class.' },
      { type: 'practice', id: 'd14-p3', lang: 'java', title: 'Practice: ArrayList Toolbox', starter: 'import java.util.*;\npublic class Toolbox {\n    public static void main(String[] args) {\n        ArrayList<String> fruits = new ArrayList<>();\n\n        // TODO 1: add Apple, Banana, Cherry, Date (in that order)\n        // TODO 2: insert "Mango" at index 1\n        // TODO 3: print the list (what is the order now?)\n        // TODO 4: print the element at index 2\n        // TODO 5: replace index 3 with "Kiwi"\n        // TODO 6: remove "Apple" by value, then remove index 0 by index\n        // TODO 7: print size, then check contains("Mango")\n        // TODO 8: clear the list and print it\n    }\n}', hint: 'add(1, "Mango") inserts. set(3, "Kiwi") replaces. remove("Apple") by value, remove(0) by index. contains() returns boolean. After clear() the list is [].' },
      { type: 'practice', id: 'd14-p4', lang: 'java', title: 'Practice: Sort by Length', starter: 'import java.util.*;\npublic class SortByLength {\n    public static void main(String[] args) {\n        ArrayList<String> words = new ArrayList<>(Arrays.asList(\n            \"elephant\", \"ant\", \"giraffe\", \"bee\", \"hippopotamus\", \"cat\"\n        ));\n\n        // TODO 1: sort by natural order (alphabetical) and print\n        // TODO 2: sort by LENGTH (shortest first) using a Comparator\n        // TODO 3: sort by length DESCENDING (longest first)\n        // TODO 4: sort by length, and for ties alphabetically\n        //         (use .thenComparing())\n    }\n}', hint: 'Collections.sort(words) for natural. words.sort(Comparator.comparingInt(String::length)) for length. .reversed() for desc. Chain: comparingInt(String::length).thenComparing(Comparator.naturalOrder()).' },
      { type: 'practice', id: 'd14-p5', lang: 'java', title: 'Practice: CompareTo Explorer', starter: 'public class CompareToExplorer {\n    public static void main(String[] args) {\n        // TODO 1: print the result of "apple".compareTo("banana")\n        // TODO 2: print "banana".compareTo("apple")\n        // TODO 3: print "apple".compareTo("apple")\n        // TODO 4: print "Apple".compareTo("apple\") — case matters!\n        // TODO 5: print Integer.valueOf(5).compareTo(10)\n        // TODO 6: in comments, write the RULE for what compareTo returns\n        //         (negative / zero / positive means what?)\n    }\n}', hint: 'compareTo returns: negative if this < other, 0 if equal, positive if this > other. "apple" vs "banana" is negative. \'A\' (65) < \'a\' (97), so "Apple".compareTo("apple") is negative.' },
      { type: 'practice', id: 'd14-p6', lang: 'java', title: 'Practice: Cricket Team Ranking', starter: 'import java.util.*;\npublic class CricketRanking {\n    public static void main(String[] args) {\n        ArrayList<Player> team = new ArrayList<>();\n        team.add(new Player("Virat", 58));\n        team.add(new Player("Rohit", 48));\n        team.add(new Player("Jasprit", 28));\n        team.add(new Player("KL", 40));\n\n        // TODO 1: sort by runs DESCENDING (top scorer first) and print\n        // TODO 2: sort by name ALPHABETICAL and print\n        // TODO 3: find and print the top scorer (first element after sort)\n        // TODO 4: print the player with the FEWEST runs (last element)\n    }\n}\nclass Player {\n    String name; int runs;\n    Player(String n, int r) { name=n; runs=r; }\n    public String toString() { return name + \" (\" + runs + \")\"; }\n}', hint: 'Sort desc: team.sort(Comparator.comparingInt(Player::getRuns).reversed()). Add a getRuns() getter. After sorting desc, index 0 is the top scorer and the last index is the lowest.' },
    ],
    tasks: [
      { id: 'java-14-d14-t1', text: 'ArrayList of Student objects. Demonstrate add, get, set, remove, contains, iteration.', tag: 'lab' },
      { id: 'java-14-d14-t2', text: 'Implement Comparable<Student> to sort by roll number. Print sorted list.', tag: 'lab' },
      { id: 'java-14-d14-t3', text: 'Two Comparators: sort by name, then sort by GPA descending.', tag: 'lab' },
      { id: 'java-14-d14-t4', text: 'Ranking system: sort by GPA desc, then by name for ties. Print with rank numbers.', tag: 'bonus' },
    ],
  },
];
