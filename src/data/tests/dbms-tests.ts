import type { Reference } from '../../types';

export const dbmsTests: Reference[] = [
  {
    id: 'dbms-test-1', title: 'Test 1: DDL, DQL & Basic Queries', category: 'DBMS',
    description: 'CREATE, SELECT, WHERE, DISTINCT, data types, NULL handling. 20 questions — target 16+.',
    blocks: [{ type: 'quiz', id: 'dbms-test-1-quiz', title: 'DDL, DQL & Basic Queries (Days 1-3)', questions: [
      { id: 'dt1-q1', question: 'Which category does CREATE TABLE belong to?', options: ['DML', 'DDL', 'DQL', 'DCL'], correctIndex: 1, explanation: 'DDL = Data Definition Language. CREATE, ALTER, DROP, TRUNCATE define database structure.' },
      { id: 'dt1-q2', question: 'How do you check if a column value is NULL?', options: ['WHERE col = NULL', 'WHERE col IS NULL', 'WHERE col == NULL', 'WHERE col EQUALS NULL'], correctIndex: 1, explanation: 'NULL means "unknown" — cannot be compared with =. Always use IS NULL or IS NOT NULL.' },
      { id: 'dt1-q3', question: 'Are SQL keywords case-sensitive?', options: ['Yes, must be uppercase', 'No, case-insensitive', 'Only on Linux', 'Only SELECT is case-sensitive'], correctIndex: 1, explanation: 'SQL keywords are case-insensitive. SELECT, select, SeLeCt all work. Table/database names may be case-sensitive on some OS.' },
      { id: 'dt1-q4', question: 'What does VARCHAR(20) mean?', options: ['Exactly 20 characters required', 'Up to 20 characters, variable length', '20 bytes always', '20 rows'], correctIndex: 1, explanation: 'VARCHAR(n) = variable-length string with max n characters. Only uses storage for actual text length, unlike CHAR(n).' },
      { id: 'dt1-q5', question: 'What does SELECT DISTINCT species FROM pet return?', options: ['All rows', 'Only unique species values, no duplicates', 'The first row for each species', 'Count of each species'], correctIndex: 1, explanation: 'DISTINCT removes duplicate rows. Returns each unique value once.' },
      { id: 'dt1-q6', question: 'What does the semicolon (;) do in SQL?', options: ['Nothing — optional', 'Terminates an SQL statement', 'Starts a comment', 'Separates columns'], correctIndex: 1, explanation: 'Semicolon terminates an SQL statement. In MySQL CLI, the statement will not execute until ; is entered.' },
      { id: 'dt1-q7', question: 'Which data type is best for storing a person\'s age?', options: ['VARCHAR(3)', 'INT', 'DATE', 'CHAR(3)'], correctIndex: 1, explanation: 'INT for whole numbers. VARCHAR is text. DATE stores full dates. CHAR is fixed-length text.' },
      { id: 'dt1-q8', question: 'What does SHOW TABLES do?', options: ['Shows table structure', 'Lists all tables in the current database', 'Displays table data', 'Creates a new table'], correctIndex: 1, explanation: 'SHOW TABLES lists all tables in the currently selected database. DESCRIBE table_name shows structure.' },
      { id: 'dt1-q9', question: 'What does DESCRIBE pet output?', options: ['All rows in the pet table', 'Column names, types, and constraints', 'The CREATE TABLE statement', 'A count of rows'], correctIndex: 1, explanation: 'DESCRIBE shows the table structure: column names, data types, NULL allowance, keys, default values.' },
      { id: 'dt1-q10', question: 'Which operator finds rows WHERE a column matches a pattern?', options: ['=', 'IN', 'LIKE', 'BETWEEN'], correctIndex: 2, explanation: 'LIKE with % (any chars) and _ (single char). WHERE name LIKE \'J%\' finds names starting with J.' },
      { id: 'dt1-q11', question: 'What does ORDER BY birth DESC do?', options: ['Sorts oldest first', 'Sorts youngest first (descending)', 'Removes duplicate births', 'Groups by birth'], correctIndex: 1, explanation: 'DESC = descending (newest/highest first). ASC = ascending (oldest/lowest first). ASC is default if not specified.' },
      { id: 'dt1-q12', question: 'What does LIMIT 5 do?', options: ['Skips first 5 rows', 'Returns only the first 5 rows', 'Limits to 5 columns', 'Shows every 5th row'], correctIndex: 1, explanation: 'LIMIT n restricts the result set to the first n rows. LIMIT 10,5 skips 10 rows and returns the next 5.' },
      { id: 'dt1-q13', question: 'What is the port conflict fix for XAMPP MySQL when Apache uses the same port?', options: ['Restart the computer', 'Change MySQL port in my.ini and connect with mysql -P newport', 'Uninstall Apache', 'Disable the firewall'], correctIndex: 1, explanation: 'Port 3306 conflict? Change both port lines in my.ini. Connect with mysql -u root -p -P 3307 (capital -P for port).' },
      { id: 'dt1-q14', question: 'What does SELECT * do vs SELECT specific columns?', options: ['Only SELECT * works', 'SELECT * is faster', 'SELECT * returns all columns. Specific columns is more efficient and clearer.', 'SELECT * returns only the first column'], correctIndex: 2, explanation: 'SELECT * gets all columns — fine for exploration. Specific columns are faster (less data transfer) and more maintainable (won\'t break if columns are added).' },
      { id: 'dt1-q15', question: 'What does WHERE species = \'dog\' AND sex = \'f\' do?', options: ['Returns rows matching EITHER condition', 'Returns rows matching BOTH conditions', 'Returns rows where species is NOT dog', 'Returns an error'], correctIndex: 1, explanation: 'AND = both conditions must be true. OR = at least one condition must be true. NOT = reverses the condition.' },
      { id: 'dt1-q16', question: 'What is the purpose of USE database_name?', options: ['Creates a database', 'Selects a database as the active/current database', 'Grants database permissions', 'Deletes the database'], correctIndex: 1, explanation: 'USE selects the active database. All subsequent queries operate on this database until you USE another one or disconnect.' },
      { id: 'dt1-q17', question: 'What does NULL mean in SQL?', options: ['Zero', 'Empty string', 'Unknown or no value', 'Default value'], correctIndex: 2, explanation: 'NULL means "unknown" or "no value." NOT the same as 0, empty string \'\', or false. NULL = NULL evaluates to UNKNOWN, not TRUE.' },
      { id: 'dt1-q18', question: 'What does BETWEEN 10 AND 20 include in MySQL?', options: ['10 and 20 (inclusive)', '11 through 19 (exclusive)', '10 only', '20 only'], correctIndex: 0, explanation: 'BETWEEN is INCLUSIVE. WHERE age BETWEEN 10 AND 20 returns 10, 11, ..., 20. Both endpoints included.' },
      { id: 'dt1-q19', question: 'What does WHERE city IN (\'Mumbai\', \'Delhi\', \'Chennai\') do?', options: ['Checks if city is any of the three values', 'Checks if city is between the three values', 'Checks if city is NOT any of the values', 'Creates a new table'], correctIndex: 0, explanation: 'IN checks membership in a list. Equivalent to: WHERE city=\'Mumbai\' OR city=\'Delhi\' OR city=\'Chennai\'. Cleaner and faster.' },
      { id: 'dt1-q20', question: 'How do you escape a single quote inside a SQL string?', options: ['Use double quotes', 'Double the quote: \'It\'\'s\'', 'Use backslash: \'It\\\'s\'', 'Single quotes cannot appear in strings'], correctIndex: 1, explanation: 'Double single quotes: \'It\'\'s a dog\'. Or use double quotes for the string: "It\'s a dog". MySQL supports both.' },
    ] }],
  },

  {
    id: 'dbms-test-2', title: 'Test 2: DML, Joins & Subqueries', category: 'DBMS',
    description: 'INSERT, UPDATE, DELETE, INNER/LEFT/RIGHT JOIN, subqueries, foreign keys. 20 questions.',
    blocks: [{ type: 'quiz', id: 'dbms-test-2-quiz', title: 'DML, Joins & Subqueries (Days 4-5)', questions: [
      { id: 'dt2-q1', question: 'Which command adds new rows to a table?', options: ['ADD', 'INSERT INTO', 'UPDATE', 'CREATE ROW'], correctIndex: 1, explanation: 'INSERT INTO adds new rows. UPDATE modifies existing rows. DELETE removes rows. CREATE TABLE defines structure.' },
      { id: 'dt2-q2', question: 'What happens if you run UPDATE without WHERE?', options: ['Nothing — it is invalid', 'ALL rows are updated', 'The first row is updated', 'An error is thrown'], correctIndex: 1, explanation: 'UPDATE without WHERE updates EVERY row in the table. Always test with SELECT first to verify which rows will be affected.' },
      { id: 'dt2-q3', question: 'What does INNER JOIN return?', options: ['All rows from both tables', 'Only rows with matching values in BOTH tables', 'All rows from the left table', 'All rows from the right table'], correctIndex: 1, explanation: 'INNER JOIN returns only the rows where the join condition matches in BOTH tables. It is the intersection of the two tables.' },
      { id: 'dt2-q4', question: 'What does LEFT JOIN return when there is NO matching row in the right table?', options: ['The row is excluded', 'The row appears with NULL values for right-table columns', 'An error is thrown', 'A default row is created'], correctIndex: 1, explanation: 'LEFT JOIN keeps ALL left-table rows. Unmatched right columns show NULL. Use WHERE right.id IS NULL to find orphan rows.' },
      { id: 'dt2-q5', question: 'What does DELETE vs TRUNCATE do?', options: ['DELETE removes structure; TRUNCATE removes data', 'DELETE can use WHERE for selective removal; TRUNCATE removes ALL rows instantly', 'They are identical', 'TRUNCATE is slower'], correctIndex: 1, explanation: 'DELETE = surgical with WHERE clause (row-by-row). TRUNCATE = removes ALL rows instantly, resets AUTO_INCREMENT. TRUNCATE cannot be rolled back.' },
      { id: 'dt2-q6', question: 'What is a foreign key?', options: ['The main identifier for a row', 'A column that references a primary key in another table', 'An encrypted key', 'A unique constraint'], correctIndex: 1, explanation: 'Foreign Key references the Primary Key of another table. Enforces referential integrity — you cannot insert a child row referencing a non-existent parent.' },
      { id: 'dt2-q7', question: 'What does ON DELETE CASCADE do?', options: ['Prevents deletion of parent rows', 'Automatically deletes child rows when the parent is deleted', 'Logs the deletion', 'Creates a backup before deletion'], correctIndex: 1, explanation: 'CASCADE: deleting a parent automatically deletes all child rows. Without it, you cannot delete a parent if child rows exist (FK constraint violation).' },
      { id: 'dt2-q8', question: 'Which JOIN type would you use to find pets WITHOUT an owner?', options: ['INNER JOIN', 'LEFT JOIN ... WHERE right.id IS NULL', 'CROSS JOIN', 'RIGHT JOIN only'], correctIndex: 1, explanation: 'LEFT JOIN keeps all pets. WHERE owners.id IS NULL filters to only pets with no matching owner — the "orphan" pattern.' },
      { id: 'dt2-q9', question: 'What is a subquery?', options: ['A backup query', 'A SELECT query nested inside another query', 'A query on a subset of columns', 'A query that only reads'], correctIndex: 1, explanation: 'Subquery = query-within-a-query. Can be in WHERE (IN, EXISTS), FROM (derived table), or SELECT (scalar subquery). Inner query runs first.' },
      { id: 'dt2-q10', question: 'What does WHERE owner_id IN (SELECT owner_id FROM owners WHERE city = \'Mumbai\') do?', options: ['Joins two tables', 'Returns pets whose owners live in Mumbai', 'Deletes pets in Mumbai', 'Creates a new table'], correctIndex: 1, explanation: 'The subquery finds owner_ids in Mumbai. The outer query returns pets matching those owner_ids. IN = membership check.' },
      { id: 'dt2-q11', question: 'What is the difference between ON and WHERE in a JOIN query?', options: ['No difference', 'ON defines the join condition. WHERE filters rows AFTER the join.', 'ON is for INNER JOIN only', 'WHERE is for LEFT JOIN only'], correctIndex: 1, explanation: 'ON specifies how tables connect (the join criteria). WHERE filters the joined result set. Different execution timing with OUTER JOINs.' },
      { id: 'dt2-q12', question: 'What happens if you insert a row with a foreign key value that does not exist in the parent table?', options: ['The row is inserted with a warning', 'The INSERT fails — referential integrity violation', 'NULL is inserted instead', 'A new parent row is auto-created'], correctIndex: 1, explanation: 'FK constraint prevents orphan rows. INSERT fails with error: "Cannot add or update a child row: a foreign key constraint fails."' },
      { id: 'dt2-q13', question: 'Which DELETE statement removes ALL rows safely?', options: ['DELETE pet;', 'DELETE FROM pet;', 'DELETE * FROM pet;', 'REMOVE FROM pet;'], correctIndex: 1, explanation: 'DELETE FROM table_name removes all rows (but is slow — row by row). TRUNCATE TABLE pet is faster but non-reversible and resets auto-increment.' },
      { id: 'dt2-q14', question: 'What does INSERT INTO pet (name, species) VALUES (\'Buddy\', \'dog\') do to unspecified columns?', options: ['Sets them to NULL', 'Uses default values if defined, otherwise NULL', 'Throws an error', 'Copies from the previous row'], correctIndex: 1, explanation: 'Unspecified columns get their DEFAULT value if defined, otherwise NULL. If a column is NOT NULL with no default, the INSERT fails.' },
      { id: 'dt2-q15', question: 'What is a self-join?', options: ['Joining a table to a copy of itself', 'A join that returns the same row twice', 'A join with no condition', 'A deprecated feature'], correctIndex: 0, explanation: 'Self-join: a table joins to itself using aliases. Example: employees e1 JOIN employees e2 ON e1.manager_id = e2.employee_id.' },
      { id: 'dt2-q16', question: 'What does a correlated subquery do that a regular subquery does not?', options: ['Run only once', 'Reference the outer query — executes once per outer row', 'Use JOINs internally', 'Return multiple columns'], correctIndex: 1, explanation: 'Correlated subquery references outer query columns. Runs ONCE PER OUTER ROW. Regular subquery runs ONCE independently. Example: WHERE salary > (SELECT AVG(salary) FROM emp e2 WHERE e2.dept = e1.dept).' },
      { id: 'dt2-q17', question: 'How do you safely test an UPDATE before executing it?', options: ['Run the UPDATE and hope', 'Run the same query as SELECT first to see which rows will be affected', 'Use a transaction', 'Copy the table first'], correctIndex: 1, explanation: 'Test with SELECT using the SAME WHERE clause. Verify the rows. Then run UPDATE with the identical WHERE clause. Transactions (START TRANSACTION / ROLLBACK) are the safety net.' },
      { id: 'dt2-q18', question: 'What does RIGHT JOIN do?', options: ['Same as LEFT JOIN', 'Keeps ALL rows from the RIGHT table, matching left where possible', 'Joins only identical rows', 'Is not valid SQL'], correctIndex: 1, explanation: 'RIGHT JOIN = keeps all rows from the RIGHT table (after JOIN keyword). LEFT is from the table in FROM. LEFT is more common and readable.' },
      { id: 'dt2-q19', question: 'What is a composite primary key?', options: ['A key made of one column', 'A primary key consisting of MULTIPLE columns together', 'An auto-increment key', 'A foreign key'], correctIndex: 1, explanation: 'Composite PK = multiple columns together uniquely identify a row. Example: (student_id, course_id) in an enrollment table.' },
      { id: 'dt2-q20', question: 'What does UPDATE pet SET owner_id = (SELECT ...) WHERE ... require?', options: ['A JOIN', 'The subquery to return a SINGLE value (scalar)', 'A trigger', 'A stored procedure'], correctIndex: 1, explanation: 'Subquery in SET must return a SINGLE scalar value. If it returns multiple rows → error. Use LIMIT 1 or ensure uniqueness with WHERE.' },
    ] }],
  },

  {
    id: 'dbms-test-3', title: 'Test 3: Aggregates, Grouping & Theory', category: 'DBMS',
    description: 'COUNT/AVG/SUM, GROUP BY, HAVING, ACID, normalization. 20 questions.',
    blocks: [{ type: 'quiz', id: 'dbms-test-3-quiz', title: 'Aggregates, Grouping & Theory (Days 6-8)', questions: [
      { id: 'dt3-q1', question: 'What is the difference between COUNT(*) and COUNT(column)?', options: ['No difference', 'COUNT(*) counts all rows. COUNT(column) counts only non-NULL values.', 'COUNT(column) is faster', 'COUNT(*) counts only NULLs'], correctIndex: 1, explanation: 'COUNT(*) counts every row regardless of NULL. COUNT(column) skips NULL values. Know this for exams.' },
      { id: 'dt3-q2', question: 'What is the difference between WHERE and HAVING?', options: ['No difference', 'WHERE filters rows BEFORE grouping. HAVING filters groups AFTER aggregation.', 'HAVING is faster', 'WHERE is for SELECT only'], correctIndex: 1, explanation: 'WHERE → GROUP BY → HAVING → SELECT → ORDER BY. HAVING can use aggregates (COUNT>5). WHERE cannot.' },
      { id: 'dt3-q3', question: 'What does \'A\' in ACID stand for?', options: ['Availability', 'Atomicity', 'Abstraction', 'Aggregation'], correctIndex: 1, explanation: 'Atomicity = transaction is all-or-nothing. Either ALL changes happen, or NONE do. Like a bank transfer: debit+credit must both succeed or both fail.' },
      { id: 'dt3-q4', question: 'What does the \'I\' in ACID stand for?', options: ['Integrity', 'Isolation', 'Indexing', 'Inheritance'], correctIndex: 1, explanation: 'Isolation = concurrent transactions do not interfere. Each sees a consistent snapshot as if it were the only transaction running.' },
      { id: 'dt3-q5', question: 'What is 1NF (First Normal Form)?', options: ['No duplicate rows', 'All columns contain atomic values, no repeating groups', 'All non-key columns depend on the full primary key', 'No transitive dependencies'], correctIndex: 1, explanation: '1NF = atomic values only. No multi-value columns or repeating groups. Each cell contains exactly one value.' },
      { id: 'dt3-q6', question: 'What is 2NF (Second Normal Form)?', options: ['Atomic values only', 'No partial dependencies — all non-key columns depend on the FULL primary key', 'No transitive dependencies', 'All foreign keys are indexed'], correctIndex: 1, explanation: '2NF = 1NF + no partial dependencies. All non-key attributes must depend on the ENTIRE primary key, not just part of it.' },
      { id: 'dt3-q7', question: 'What problem does 3NF (Third Normal Form) solve?', options: ['Duplicate rows', 'Transitive dependencies (non-key → non-key)', 'NULL values', 'Slow queries'], correctIndex: 1, explanation: '3NF = 2NF + no transitive dependencies. No non-key column should depend on another non-key column. Example: emp_id→dept_id→dept_name — dept_name should be in a separate departments table.' },
      { id: 'dt3-q8', question: 'How do you calculate age from a birth date in MySQL?', options: ['age(birth)', 'TIMESTAMPDIFF(YEAR, birth, CURDATE())', 'birth - CURDATE()', 'DATEDIFF(CURDATE(), birth)'], correctIndex: 1, explanation: 'TIMESTAMPDIFF(unit, start_date, end_date). Unit can be YEAR, MONTH, DAY, HOUR, etc. CURDATE() is current date. For lifespan: replace CURDATE() with death_date.' },
      { id: 'dt3-q9', question: 'What does GROUP BY species do?', options: ['Sorts by species', 'Groups rows with identical species values, enabling per-group aggregation', 'Filters by species', 'Removes species column'], correctIndex: 1, explanation: 'GROUP BY creates groups for each unique value. Aggregate functions (COUNT, AVG) then operate per group. Each row in the result represents one group.' },
      { id: 'dt3-q10', question: 'Can you use COUNT() in a WHERE clause?', options: ['Yes — WHERE COUNT(*) > 5', 'No — aggregate functions cannot be in WHERE. Use HAVING.', 'Only with subqueries', 'Only in MySQL 8+'], correctIndex: 1, explanation: 'Aggregate functions are evaluated AFTER WHERE (during the GROUP BY phase). Use HAVING for filtering aggregates: HAVING COUNT(*) > 5.' },
      { id: 'dt3-q11', question: 'What does ALTER TABLE pet ADD COLUMN color VARCHAR(20) do?', options: ['Changes pet data', 'Adds a new column to the existing table', 'Renames the table', 'Creates a new table'], correctIndex: 1, explanation: 'ADD COLUMN adds a new column. Existing rows get NULL (or DEFAULT if specified). MODIFY COLUMN changes type. DROP COLUMN removes a column.' },
      { id: 'dt3-q12', question: 'Can a primary key be NULL?', options: ['Yes', 'No — primary keys must be NOT NULL', 'Only on composite keys', 'Only if it is auto-increment'], correctIndex: 1, explanation: 'Primary key = UNIQUE + NOT NULL. Every row must have a PK value. Foreign keys CAN be NULL (meaning no relationship).' },
      { id: 'dt3-q13', question: 'What is referential integrity?', options: ['Data backup integrity', 'Foreign key values must reference existing primary key values (or be NULL)', 'Query accuracy', 'Index maintenance'], correctIndex: 1, explanation: 'Referential integrity: FK values must match existing PK values (or be NULL). Prevents orphan records. Enforced by FOREIGN KEY constraint.' },
      { id: 'dt3-q14', question: 'What MySQL prompt does \'"> mean and how do you escape it?', options: ['Ready for query', 'Waiting for closing double quote — type " then ;', 'Multi-line continuation', 'Connection lost'], correctIndex: 1, explanation: '\'"> means you have an unclosed double quote. Type " to close the string, then ; to execute. Or type \\c to cancel.' },
      { id: 'dt3-q15', question: 'What does TRUNCATE TABLE do that DELETE FROM does not?', options: ['Delete specific rows', 'Reset AUTO_INCREMENT counter', 'Use WHERE clause', 'Rollback support'], correctIndex: 1, explanation: 'TRUNCATE resets AUTO_INCREMENT to 1. DELETE does not. TRUNCATE is faster (deallocates data pages) but cannot be rolled back in most cases.' },
      { id: 'dt3-q16', question: 'What is the purpose of AUTO_INCREMENT?', options: ['Speed up queries', 'Automatically generate unique sequential values for a column', 'Create backups', 'Sort data automatically'], correctIndex: 1, explanation: 'AUTO_INCREMENT generates the next sequential value automatically on INSERT. Typically used with PRIMARY KEY. Resets after TRUNCATE.' },
      { id: 'dt3-q17', question: 'What does DROP TABLE IF EXISTS pet do?', options: ['Deletes all rows from pet', 'Deletes the table structure and data if it exists; no error if it does not', 'Renames the table', 'Creates the table if it does not exist'], correctIndex: 1, explanation: 'DROP TABLE removes structure + data. IF EXISTS prevents error if the table does not exist. Safer than plain DROP TABLE.' },
      { id: 'dt3-q18', question: 'Which normalization level eliminates transitive dependencies?', options: ['1NF', '2NF', '3NF', 'BCNF'], correctIndex: 2, explanation: '3NF eliminates transitive dependencies — when a non-key attribute depends on another non-key attribute instead of directly on the PK.' },
      { id: 'dt3-q19', question: 'What is a primary key?', options: ['Any unique column', 'A column (or set of columns) that UNIQUELY identifies each row. NOT NULL. Exactly one per table.', 'A foreign key in the parent table', 'An indexed column'], correctIndex: 1, explanation: 'Primary Key = unique identifier for each row. Must be UNIQUE and NOT NULL. One PK per table. Often AUTO_INCREMENT. Foreign keys reference PKs.' },
      { id: 'dt3-q20', question: 'What does AVG(column) return for a column with NULLs?', options: ['NULL', 'Average of non-NULL values. NULLs are ignored.', '0', 'An error'], correctIndex: 1, explanation: 'AVG (like SUM, MIN, MAX, COUNT(col)) ignores NULL values. Only COUNT(*) counts rows with NULLs. Be aware when calculating averages.' },
    ] }],
  },

  {
    id: 'dbms-test-4', title: 'Test 4: Views, Indexes & Programming', category: 'DBMS',
    description: 'Views, indexes, EXPLAIN, procedures, functions, triggers. 20 questions.',
    blocks: [{ type: 'quiz', id: 'dbms-test-4-quiz', title: 'Views, Indexes & Programming (Days 9-10)', questions: [
      { id: 'dt4-q1', question: 'Does a view store data?', options: ['Yes — it is a physical copy', 'No — it is a stored query that runs when accessed', 'Only if you specify MATERIALIZED', 'Views store indexes only'], correctIndex: 1, explanation: 'Views are VIRTUAL — they store the SELECT statement, not data. Each query on a view runs the underlying SELECT. MySQL does not support materialized views.' },
      { id: 'dt4-q2', question: 'What is the main benefit of an index?', options: ['Reduces disk space', 'Speeds up SELECT queries on indexed columns', 'Reduces memory usage', 'Prevents duplicate data'], correctIndex: 1, explanation: 'Indexes create a B-tree structure that allows O(log n) lookup instead of O(n) full table scan. Dramatic speed improvements for large tables.' },
      { id: 'dt4-q3', question: 'What is the downside of adding too many indexes?', options: ['SELECT queries slow down', 'INSERT, UPDATE, and DELETE slow down — must update all indexes', 'Database cannot start', 'Indexes do not work after 5'], correctIndex: 1, explanation: 'Each INSERT/UPDATE/DELETE must maintain ALL indexes. More indexes = slower writes. Also, indexes consume disk space and memory (buffer pool).' },
      { id: 'dt4-q4', question: 'What does EXPLAIN output help you understand?', options: ['Syntax errors in your query', 'How MySQL will execute your query — whether indexes are used', 'The database schema', 'User permissions'], correctIndex: 1, explanation: 'EXPLAIN shows the execution plan. Key columns: type (ALL=bad, ref=good), key (which index used), rows (estimated examined). Use to optimize queries.' },
      { id: 'dt4-q5', question: 'A composite index on (A, B). Which query CAN use this index?', options: ['WHERE B = 5', 'WHERE A = 10', 'WHERE A = 10 AND B = 5', 'Both B and C'], correctIndex: 2, explanation: 'Leftmost prefix rule: (A, B) supports queries on A alone or A+B. NOT on B alone. The index is like (last_name, first_name) — you cannot search just by first_name efficiently.' },
      { id: 'dt4-q6', question: 'Why do you need DELIMITER when creating stored procedures?', options: ['It makes procedures run faster', 'Without it, MySQL would treat ; inside the procedure as end of CREATE PROCEDURE', 'It is a security feature', 'It is optional convention'], correctIndex: 1, explanation: 'The procedure body contains semicolons. Changing the delimiter (to // or $$) prevents MySQL from interpreting those semicolons as the end of the CREATE PROCEDURE statement.' },
      { id: 'dt4-q7', question: 'What is the difference between a stored procedure and a function?', options: ['No difference', 'Functions RETURN a value and can be used in SELECT. Procedures use CALL.', 'Procedures are faster', 'Functions can modify data'], correctIndex: 1, explanation: 'Functions: RETURN value, used in SELECT, usually read-only. Procedures: use CALL, can have OUT parameters, CAN modify data (INSERT/UPDATE/DELETE).' },
      { id: 'dt4-q8', question: 'When does a BEFORE INSERT trigger fire?', options: ['After the row is inserted', 'Before the row is written — can modify NEW values', 'During transaction commit', 'Only if the insert fails'], correctIndex: 1, explanation: 'BEFORE INSERT fires BEFORE the row is written. You can modify NEW.column values. Use for validation, data transformation, or auto-setting fields.' },
      { id: 'dt4-q9', question: 'In a DELETE trigger, which pseudo-row is available?', options: ['NEW only', 'OLD only', 'Both OLD and NEW', 'Neither'], correctIndex: 1, explanation: 'DELETE trigger: only OLD is available (the row being deleted). INSERT: only NEW. UPDATE: both OLD and NEW.' },
      { id: 'dt4-q10', question: 'What does CREATE UNIQUE INDEX do?', options: ['Creates a normal index', 'Creates an index that also ENFORCES uniqueness on the column(s)', 'Creates a temporary index', 'Drops existing indexes'], correctIndex: 1, explanation: 'UNIQUE INDEX = index + uniqueness constraint. Prevents duplicate values. Useful for email, username, ISBN columns.' },
      { id: 'dt4-q11', question: 'Can you CREATE INDEX on a view?', options: ['Yes', 'No — views do not store data. Index the underlying table instead.', 'Only on materialized views', 'Only with special permissions'], correctIndex: 1, explanation: 'Views are virtual — no data to index. Index the underlying base tables. The index will automatically benefit queries through the view.' },
      { id: 'dt4-q12', question: 'What does SIGNAL SQLSTATE \'45000\' do in a trigger?', options: ['Logs a message', 'Raises a custom error — stops the operation', 'Sends an email', 'Begins a transaction'], correctIndex: 1, explanation: 'SIGNAL raises a custom error/exception. SQLSTATE \'45000\' = user-defined exception. Used to PREVENT an operation (e.g., stop deletion if condition is met).' },
      { id: 'dt4-q13', question: 'What does type=ALL mean in EXPLAIN output?', options: ['The query uses all indexes', 'Full table scan — no index used. SLOW for large tables.', 'All rows are returned', 'The query is fully optimized'], correctIndex: 1, explanation: 'type=ALL = full table scan. MySQL reads every row. Bad for performance on large tables. Add an index on the WHERE or JOIN columns.' },
      { id: 'dt4-q14', question: 'What is a covering index?', options: ['An index that covers all columns', 'An index that contains ALL columns needed by the query — no table access needed', 'An index on foreign keys', 'A unique index'], correctIndex: 1, explanation: 'Covering index = index includes all columns in the SELECT + WHERE. EXPLAIN shows "Using index" in Extra. The query is satisfied entirely from the index — fastest possible.' },
      { id: 'dt4-q15', question: 'What are IN, OUT, and INOUT in stored procedures?', options: ['Loop controls', 'Parameter modes: IN = input, OUT = output, INOUT = both', 'Error codes', 'Table aliases'], correctIndex: 1, explanation: 'IN = input parameter (read-only, default). OUT = output parameter (procedure sets it, caller reads it). INOUT = both input and output.' },
      { id: 'dt4-q16', question: 'How do you call a procedure with an OUT parameter?', options: ['CALL proc(@var); SELECT @var;', 'SELECT proc(@var);', 'EXEC proc(@var);', 'CALL proc(?, @var);'], correctIndex: 0, explanation: 'Use @variable for OUT parameters: CALL CountPets(\'dog\', @count); SELECT @count;. @variable is a MySQL session variable.' },
      { id: 'dt4-q17', question: 'What does AFTER UPDATE trigger give you access to?', options: ['Only the new values', 'Only the old values', 'Both OLD (before) and NEW (after) values', 'Neither — triggers cannot access row data'], correctIndex: 2, explanation: 'AFTER UPDATE: both OLD.column (pre-update value) and NEW.column (post-update value) are available. Useful for audit logging and change tracking.' },
      { id: 'dt4-q18', question: 'What is the difference between CHAR and VARCHAR?', options: ['No difference', 'CHAR is fixed-length (padded with spaces). VARCHAR is variable-length.', 'VARCHAR is always faster', 'CHAR is deprecated'], correctIndex: 1, explanation: 'CHAR(n) stores exactly n characters (padded). VARCHAR(n) stores up to n, using only needed space. Use CHAR for fixed-length codes.' },
      { id: 'dt4-q19', question: 'What is a full-text index used for?', options: ['Speeding up JOINs', 'Efficient text search with MATCH...AGAINST', 'Enforcing uniqueness', 'Sorting text columns'], correctIndex: 1, explanation: 'FULLTEXT index enables MATCH(col1, col2) AGAINST(\'search terms\') — natural language search with relevance ranking. For articles, descriptions, documents.' },
      { id: 'dt4-q20', question: 'Can triggers cause an infinite loop?', options: ['No — MySQL prevents it', 'Yes — an UPDATE trigger that updates the same table can trigger itself', 'Only in stored procedures', 'Only in MySQL 5.7+'], correctIndex: 1, explanation: 'A trigger that performs an operation on its own table can recursively trigger itself. MySQL has max_sp_recursion_depth to limit this (default 0 = no recursion).' },
    ] }],
  },

  {
    id: 'dbms-test-5', title: 'Test 5: DBMS Full Course Review', category: 'DBMS',
    description: 'Mixed questions from all 10 days. Comprehensive end-of-course assessment. 20 questions.',
    blocks: [{ type: 'quiz', id: 'dbms-test-5-quiz', title: 'DBMS Speedrun — Full Course Review', questions: [
      { id: 'dt5-q1', question: 'Which SQL statement removes ALL rows from a table but keeps the structure?', options: ['DELETE FROM pet;', 'TRUNCATE TABLE pet;', 'DROP TABLE pet;', 'REMOVE FROM pet;'], correctIndex: 1, explanation: 'TRUNCATE removes all rows quickly and resets auto-increment. DELETE also works but is slower. DROP removes structure too.' },
      { id: 'dt5-q2', question: 'What is the difference between DROP and DELETE?', options: ['No difference', 'DROP removes TABLE structure + data. DELETE removes ROWS only.', 'DELETE is faster', 'DROP keeps the table structure'], correctIndex: 1, explanation: 'DROP = removes the table entirely (structure + data). DELETE = removes rows, table structure remains. TRUNCATE = removes all rows, structure remains.' },
      { id: 'dt5-q3', question: 'Which type of JOIN would show ALL customers and their orders, including customers with NO orders?', options: ['INNER JOIN', 'LEFT JOIN (customers LEFT JOIN orders)', 'CROSS JOIN', 'FULL OUTER JOIN'], correctIndex: 1, explanation: 'LEFT JOIN keeps all customers. Orders with no match → NULL. customers LEFT JOIN orders shows all customers regardless of orders.' },
      { id: 'dt5-q4', question: 'What is the correct order of operations in a SELECT query?', options: ['SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY', 'FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY', 'WHERE → FROM → GROUP BY → SELECT → ORDER BY', 'FROM → SELECT → WHERE → GROUP BY → HAVING'], correctIndex: 1, explanation: 'Execution: FROM (tables) → WHERE (filter rows) → GROUP BY → HAVING (filter groups) → SELECT (project columns) → ORDER BY (sort) → LIMIT.' },
      { id: 'dt5-q5', question: 'What does the Durability property in ACID mean?', options: ['Data is always available', 'Once a transaction commits, changes survive system crashes', 'Data is encrypted', 'Multiple users can access simultaneously'], correctIndex: 1, explanation: 'Durability = committed data is PERMANENT. Survives power failures, crashes. Achieved through write-ahead logging and disk persistence.' },
      { id: 'dt5-q6', question: 'Which normal form requires atomic (indivisible) values in every cell?', options: ['1NF', '2NF', '3NF', 'BCNF'], correctIndex: 0, explanation: '1NF = no repeating groups or multi-valued columns. Each cell contains exactly one atomic value. All further normal forms build on 1NF.' },
      { id: 'dt5-q7', question: 'What is a subquery in the SELECT clause used for?', options: ['Filtering rows', 'Displaying a computed/scalar value alongside each row', 'Joining tables', 'Sorting results'], correctIndex: 1, explanation: 'SELECT (SELECT AVG(marks) FROM students) AS avg; — scalar subquery adds a computed column to each row. Must return a SINGLE value per row.' },
      { id: 'dt5-q8', question: 'What is the purpose of an index on a foreign key column?', options: ['Enforce uniqueness', 'Speed up JOINs — the FK is used to link tables', 'Prevent NULL values', 'Auto-generate values'], correctIndex: 1, explanation: 'FK columns are frequently used in JOIN conditions. Indexing them dramatically speeds up JOIN queries. Many DBAs index all FK columns by default.' },
      { id: 'dt5-q9', question: 'How is a stored procedure different from a script of SQL statements?', options: ['No difference', 'Procedure is stored on the server, compiled once, callable by name', 'Procedures are slower', 'Scripts cannot have parameters'], correctIndex: 1, explanation: 'Stored procedures are server-side, compiled, callable by name, accept parameters. Scripts are client-side text files executed line-by-line.' },
      { id: 'dt5-q10', question: 'What happens if you write UPDATE pet SET name = \'Max\' (no WHERE clause)?', options: ['Only the first row is updated', 'ALL rows have name set to \'Max\'', 'An error is thrown', 'Nothing — WHERE is required'], correctIndex: 1, explanation: 'Missing WHERE = ALL rows updated. This is the most common dangerous SQL mistake. Always test with SELECT first.' },
      { id: 'dt5-q11', question: 'What is the purpose of GROUP BY with ROLLUP?', options: ['Undo the last grouping', 'Add subtotal/summary rows for each grouping level', 'Sort grouped results', 'Remove duplicate groups'], correctIndex: 1, explanation: 'ROLLUP adds subtotal rows and a grand total row. GROUP BY species WITH ROLLUP gives per-species counts PLUS a total row.' },
      { id: 'dt5-q12', question: 'Which aggregate function gives the median?', options: ['AVG()', 'MEDIAN()', 'There is no built-in MEDIAN() in MySQL — calculate with PERCENTILE or row numbering', 'QUARTILE()'], correctIndex: 2, explanation: 'MySQL has no MEDIAN() function. Use PERCENTILE_CONT(0.5) in other databases or calculate manually with ORDER BY + LIMIT + offset.' },
      { id: 'dt5-q13', question: 'What does a BEFORE DELETE trigger allow you to do?', options: ['Only log the deletion', 'Access OLD values and optionally PREVENT the deletion with SIGNAL', 'Restore deleted data', 'Speed up the deletion'], correctIndex: 1, explanation: 'BEFORE DELETE: access OLD values (the row being deleted). Can PREVENT deletion by raising SIGNAL. AFTER DELETE can log but cannot prevent.' },
      { id: 'dt5-q14', question: 'What does the error "Cannot delete or update a parent row: a foreign key constraint fails" mean?', options: ['The table does not exist', 'You are trying to delete a row that has child rows referencing it', 'The SQL syntax is wrong', 'You lack DELETE permission'], correctIndex: 1, explanation: 'FK constraint prevents deleting a parent row while child rows exist. Either delete children first, use ON DELETE CASCADE, or SET NULL.' },
      { id: 'dt5-q15', question: 'What is the difference between CURDATE() and NOW()?', options: ['No difference', 'CURDATE() = date only. NOW() = date + time.', 'NOW() is deprecated', 'CURDATE() includes time'], correctIndex: 1, explanation: 'CURDATE() → \'2026-07-27\'. NOW() → \'2026-07-27 14:30:00\'. Use appropriate function for your column type.' },
      { id: 'dt5-q16', question: 'Which clause would you use to find species with MORE THAN the average number of pets?', options: ['WHERE COUNT(*) > AVG(COUNT(*))', 'HAVING COUNT(*) > (SELECT AVG(cnt) FROM (...))', 'GROUP BY with WHERE', 'ORDER BY with LIMIT'], correctIndex: 1, explanation: 'Subquery: SELECT AVG(cnt) FROM (SELECT COUNT(*) AS cnt FROM pet GROUP BY species) t. Then: HAVING COUNT(*) > (that subquery).' },
      { id: 'dt5-q17', question: 'What is the benefit of using an INNER JOIN over a subquery in WHERE?', options: ['No difference', 'JOINs can be faster and allow selecting columns from both tables in the result', 'Subqueries are always better', 'INNER JOINs do not work with WHERE'], correctIndex: 1, explanation: 'JOINs allow selecting columns from MULTIPLE tables in the output. Subqueries in WHERE only filter. For displaying data from multiple tables: JOIN.' },
      { id: 'dt5-q18', question: 'How do you grant SELECT permission on a view but NOT the underlying table?', options: ['Not possible', 'GRANT SELECT ON view_name TO user — the user cannot access the base table directly', 'Use a stored procedure', 'Views inherit table permissions'], correctIndex: 1, explanation: 'Views enable column-level and row-level security. Grant SELECT on the view, but DENY on the table. Users see only what the view exposes.' },
      { id: 'dt5-q19', question: 'What is the purpose of void main in a Java program (cross-reference)?', options: ['It is the database entry point', 'Entry point for Java programs — the JVM calls main()', 'A SQL function', 'A database trigger'], correctIndex: 1, explanation: 'Cross-subject: public static void main(String[] args) is Java\'s entry point, not SQL. Understanding both subjects helps with full-stack development.' },
      { id: 'dt5-q20', question: 'What does mysqldump -u root -p database_name > backup.sql do?', options: ['Restores a database', 'Creates a backup — exports database structure and data to SQL file', 'Deletes the database', 'Starts the MySQL server'], correctIndex: 1, explanation: 'mysqldump creates a backup. Output is a .sql file containing CREATE and INSERT statements. Restore with: mysql -u root -p database_name < backup.sql.' },
    ] }],
  },
];