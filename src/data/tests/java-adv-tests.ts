import type { Reference } from '../../types';

export const javaAdvTests: Reference[] = [
  {
    id: 'java-adv-test-1', title: 'Test 1: JDBC Core & Connection', category: 'Advanced Java',
    description: 'JDBC architecture, DriverManager, Connection, Statement, ResultSet. 20 questions.',
    blocks: [{ type: 'quiz', id: 'ja-test-1-quiz', title: 'JDBC Core & Connection (Days 1-4)', questions: [
      { id: 'jat1-q1', question: 'What does JDBC stand for?', options: ['Java Data Binary Connector', 'Java Database Connectivity', 'Java Development Base Component', 'Java Data Bridge Controller'], correctIndex: 1, explanation: 'JDBC = Java Database Connectivity. API that lets Java applications connect to and interact with databases.' },
      { id: 'jat1-q2', question: 'What is the JDBC connection URL format for MySQL?', options: ['mysql://localhost/db', 'jdbc:mysql://localhost:3306/dbname', 'jdbc:mysql://dbname:3306', 'http://localhost:3306/dbname'], correctIndex: 1, explanation: 'Format: jdbc:mysql://host:port/database. For local: jdbc:mysql://localhost:3306/mydb. Port 3306 is MySQL default.' },
      { id: 'jat1-q3', question: 'Which class loads the MySQL JDBC driver?', options: ['com.mysql.Driver', 'com.mysql.cj.jdbc.Driver', 'java.sql.Driver', 'mysql.jdbc.Driver'], correctIndex: 1, explanation: 'com.mysql.cj.jdbc.Driver. The "cj" stands for Connector/J. Use Class.forName() to load it.' },
      { id: 'jat1-q4', question: 'Which method creates a Connection object?', options: ['Driver.connect()', 'DriverManager.getConnection(url, user, pass)', 'Connection.open()', 'JDBC.connect()'], correctIndex: 1, explanation: 'DriverManager.getConnection(url, user, password) returns a Connection. Throws SQLException if the connection fails.' },
      { id: 'jat1-q5', question: 'Which method do you use for a SELECT query?', options: ['stmt.executeUpdate(sql)', 'stmt.executeQuery(sql)', 'stmt.execute(sql)', 'stmt.runQuery(sql)'], correctIndex: 1, explanation: 'executeQuery() is for SELECT — returns ResultSet. executeUpdate() is for INSERT/UPDATE/DELETE/DDL — returns int (row count).' },
      { id: 'jat1-q6', question: 'What does executeUpdate() return for an INSERT?', options: ['true/false', 'Number of affected rows', 'The new row ID', 'A ResultSet'], correctIndex: 1, explanation: 'executeUpdate() returns int = number of rows affected. For DDL (CREATE, ALTER), returns 0.' },
      { id: 'jat1-q7', question: 'What is the column index in ResultSet.getInt(1)?', options: ['0-based (first column is 0)', '1-BASED (first column is 1)', 'Named index only', 'Column index starts at -1'], correctIndex: 1, explanation: 'ResultSet column indices are 1-BASED. rs.getInt(1) = first column. rs.getString(2) = second column. Prefer column names: rs.getString("name").' },
      { id: 'jat1-q8', question: 'What must you do BEFORE calling rs.getInt() for the first time?', options: ['Nothing — auto-positions', 'Call rs.next() to move cursor to the first row', 'Call rs.first()', 'Call rs.beforeFirst()'], correctIndex: 1, explanation: 'Cursor starts BEFORE the first row. You MUST call rs.next() to advance to the first row. rs.next() returns false when no more rows.' },
      { id: 'jat1-q9', question: 'Why should you close Connection, Statement, and ResultSet?', options: ['For security', 'To release DATABASE resources — connections, cursors, memory', 'It logs the completion', 'They auto-close'], correctIndex: 1, explanation: 'Unclosed resources = resource leak. Database has connection limits. Open ResultSets hold cursors/locks. Close in reverse order: rs → stmt → conn, or use try-with-resources.' },
      { id: 'jat1-q10', question: 'What is the correct try-with-resources pattern for JDBC?', options: ['try (Connection c = ...) { }', 'try (Connection c = ...; Statement s = ...; ResultSet r = ...) { }', 'try { } resources (Connection c = ...)', 'JDBC does not support try-with-resources'], correctIndex: 1, explanation: 'All three (Connection, Statement, ResultSet) implement AutoCloseable. Declare all in try(). They auto-close in reverse order.' },
      { id: 'jat1-q11', question: 'What does ResultSetMetaData provide?', options: ['Row data', 'Information ABOUT the ResultSet: column count, names, types', 'Query execution time', 'Connection details'], correctIndex: 1, explanation: 'ResultSetMetaData = schema info. getColumnCount(), getColumnName(i), getColumnType(i). Useful for dynamic/generic result processing.' },
      { id: 'jat1-q12', question: 'What exception is thrown if the JDBC driver class is not found?', options: ['SQLException', 'ClassNotFoundException', 'IOException', 'RuntimeException'], correctIndex: 1, explanation: 'Class.forName("com.mysql.cj.jdbc.Driver") throws ClassNotFoundException if the driver JAR is not in classpath.' },
      { id: 'jat1-q13', question: 'What is the CE-1 worth in marks for Advanced Java?', options: ['10', '20', '25', '30'], correctIndex: 1, explanation: 'CE-1 = 20 marks. 5 MCQs (5 marks) + 3 coding tasks at 5 marks each (15). Covers SQL and JDBC fundamentals.' },
      { id: 'jat1-q14', question: 'How do you add mysql-connector-j.jar to your project?', options: ['Just place it in the folder', 'Add to CLASSPATH: javac -cp ".;mysql-connector-j.jar" *.java', 'It loads automatically', 'Copy to JRE/lib'], correctIndex: 1, explanation: 'Must be in classpath for BOTH compilation and runtime. IDE: add as library. Command line: -cp or -classpath flag.' },
      { id: 'jat1-q15', question: 'What is the 5-step JDBC workflow?', options: ['Load, create, execute, process, ignore', 'Load driver → Connect → Create Statement → Execute → Process Results + Close', 'Connect → Query → Display → Save → Exit', 'Import → Connect → Run → Print → Close'], correctIndex: 1, explanation: '1. Load driver (Class.forName). 2. Connect (DriverManager.getConnection). 3. Create Statement. 4. Execute query/update. 5. Process ResultSet + Close resources.' },
      { id: 'jat1-q16', question: 'What happens if you call rs.getString(0)?', options: ['Returns the first column', 'SQLException — column index starts at 1, not 0', 'Returns null', 'Returns column name'], correctIndex: 1, explanation: 'Column index is 1-BASED. Index 0 is invalid → SQLException: "Column Index out of range." This is the #1 JDBC off-by-one error.' },
      { id: 'jat1-q17', question: 'What port does MySQL default to?', options: ['8080', '3306', '5432', '1521'], correctIndex: 1, explanation: 'MySQL = 3306. PostgreSQL = 5432. Oracle = 1521. SQL Server = 1433. Know your database ports.' },
      { id: 'jat1-q18', question: 'What does Connection.setAutoCommit(false) do?', options: ['Disables the connection', 'Starts a MANUAL transaction — statements do not auto-save', 'Commits all pending changes', 'Closes the connection'], correctIndex: 1, explanation: 'setAutoCommit(false) = start transaction. Must explicitly commit() or rollback(). Default is true (each statement auto-commits).' },
      { id: 'jat1-q19', question: 'Which method would you use to execute a CREATE TABLE via JDBC?', options: ['executeQuery()', 'executeUpdate()', 'executeTable()', 'createStatement()'], correctIndex: 1, explanation: 'DDL (CREATE, ALTER, DROP) uses executeUpdate(). Returns 0 (no rows affected, but statement executed). Same for INSERT/UPDATE/DELETE.' },
      { id: 'jat1-q20', question: 'What is the minimum attendance required for End Semester eligibility?', options: ['60%', '75%', '85%', '90%'], correctIndex: 1, explanation: 'University mandates 75% minimum. Below this = debarred from End Term exam regardless of internal marks. Track attendance weekly.' },
    ] }],
  },

  {
    id: 'java-adv-test-2', title: 'Test 2: PreparedStatement & CRUD', category: 'Advanced Java',
    description: 'PreparedStatement, SQL injection, CRUD patterns, ResultSet. 20 questions.',
    blocks: [{ type: 'quiz', id: 'ja-test-2-quiz', title: 'PreparedStatement & CRUD (Day 5)', questions: [
      { id: 'jat2-q1', question: 'What is the PRIMARY benefit of PreparedStatement over Statement?', options: ['Shorter syntax', 'Prevents SQL injection + pre-compiled for repeated use', 'Does not need a connection', 'Works with more databases'], correctIndex: 1, explanation: 'PreparedStatement = SQL injection prevention (parameters escaped) + performance (pre-compiled SQL reused with different params).' },
      { id: 'jat2-q2', question: 'How do you set parameters in a PreparedStatement?', options: ['Concatenation: sql + value', 'Placeholders ? and setXxx(index, value) methods', 'Using format strings', 'Parameters are not supported'], correctIndex: 1, explanation: '? placeholders in SQL. ps.setString(1, value), ps.setInt(1, value), etc. Index is 1-BASED (first ? = 1).' },
      { id: 'jat2-q3', question: 'What would happen with Statement + concatenation if user input is "1; DROP TABLE users;"?', options: ['Nothing — treated as text', 'The DROP TABLE command EXECUTES — table deleted (SQL injection)', 'SQLException', 'The semicolon is escaped'], correctIndex: 1, explanation: 'String concatenation treats input as SQL code. The semicolon ends the original query, then DROP TABLE executes. PreparedStatement prevents this.' },
      { id: 'jat2-q4', question: 'How does PreparedStatement prevent SQL injection?', options: ['It validates input', 'Parameters are ESCAPED and treated as VALUES, never as SQL code', 'It encrypts queries', 'It uses a firewall'], correctIndex: 1, explanation: '? placeholders tell the database "this is a VALUE, not code." The driver escapes special characters. User input can NEVER become SQL commands.' },
      { id: 'jat2-q5', question: 'What is a DAO (Data Access Object)?', options: ['A database table', 'A design pattern — separate class per entity that handles ALL database operations', 'A type of Connection', 'A SQL query builder'], correctIndex: 1, explanation: 'DAO pattern: one class per entity (BookDAO, MemberDAO) handles all CRUD operations. Separates SQL/business logic. Industry standard.' },
      { id: 'jat2-q6', question: 'Can you reuse a PreparedStatement with different parameters?', options: ['No — must create new each time', 'Yes — set new parameters and call execute again', 'Only for SELECT', 'Only within a transaction'], correctIndex: 1, explanation: 'Yes! Set new values via setXxx(), then execute again. The SQL is compiled ONCE and reused. This is a performance advantage.' },
      { id: 'jat2-q7', question: 'What does ps.setNull(1, Types.INTEGER) do?', options: ['Sets the value to 0', 'Sets the parameter to SQL NULL with proper type', 'Deletes the parameter', 'Throws an error'], correctIndex: 1, explanation: 'Some databases need the NULL type specified. Types.INTEGER, Types.VARCHAR, etc. Alternative: ps.setObject(1, null).' },
      { id: 'jat2-q8', question: 'Which PreparedStatement method executes a SELECT?', options: ['ps.executeUpdate()', 'ps.executeQuery()', 'ps.execute()', 'ps.runQuery()'], correctIndex: 1, explanation: 'executeQuery() for SELECT (returns ResultSet). executeUpdate() for INSERT/UPDATE/DELETE (returns int). Same as with Statement.' },
      { id: 'jat2-q9', question: 'What is the CRUD acronym?', options: ['Connect, Run, Update, Delete', 'CREATE, READ, UPDATE, DELETE', 'Compile, Run, Upload, Download', 'Call, Respond, Update, Destroy'], correctIndex: 1, explanation: 'CRUD = basic database operations. Create (INSERT), Read (SELECT), Update (UPDATE), Delete (DELETE). Every application does these.' },
      { id: 'jat2-q10', question: 'What does ResultSet rs = ps.executeQuery() return for an empty result?', options: ['null', 'An empty ResultSet — rs.next() returns false', 'SQLException', 'An empty list'], correctIndex: 1, explanation: 'executeQuery() always returns a ResultSet (never null). For no matching rows, rs.next() returns false immediately.' },
      { id: 'jat2-q11', question: 'How do you pass a java.util.Date to PreparedStatement?', options: ['ps.setString(1, date.toString())', 'Convert to java.sql.Date: ps.setDate(1, java.sql.Date.valueOf("2026-07-27"))', 'Directly: ps.setDate(1, utilDate)', 'Use setTimestamp only'], correctIndex: 1, explanation: 'JDBC uses java.sql.Date (date only) or java.sql.Timestamp (date+time). Convert: new java.sql.Date(utilDate.getTime()) or Date.valueOf("YYYY-MM-DD").' },
      { id: 'jat2-q12', question: 'What is the CE-2 worth in marks?', options: ['20', '25', '30', '35'], correctIndex: 1, explanation: 'CE-2 = 25 marks. 5 MCQs (5 marks) + 2 coding tasks at 5 marks (10) + 1 coding task at 10 marks (10).' },
      { id: 'jat2-q13', question: 'What is batch processing in JDBC?', options: ['Processing rows in batches', 'Sending multiple SQL statements in ONE network call (addBatch/executeBatch)', 'Processing data offline', 'Multi-threading'], correctIndex: 1, explanation: 'ps.addBatch() adds to batch. ps.executeBatch() sends ALL at once — one network round-trip instead of one per statement. Huge speedup for bulk operations.' },
      { id: 'jat2-q14', question: 'What does ps.executeBatch() return?', options: ['ResultSet', 'int[] — array of update counts for each batch entry', 'boolean', 'void'], correctIndex: 1, explanation: 'executeBatch() returns int[] — each element is the row count for that batch statement. SUCCESS_NO_INFO (-2) if count unknown. EXECUTE_FAILED (-3) if failed.' },
      { id: 'jat2-q15', question: 'What is connection pooling?', options: ['Backing up connections', 'Reusing a pool of pre-opened connections instead of creating new ones per query', 'Limiting connections to one', 'Encrypting connections'], correctIndex: 1, explanation: 'Connection creation is expensive (TCP + auth). Pooling: borrow from pool, use, return (do not close). 10-100x faster for apps with frequent queries.' },
      { id: 'jat2-q16', question: 'What is the difference between commit and rollback?', options: ['No difference', 'commit = SAVE changes permanently. rollback = UNDO all changes since last commit.', 'commit is for reads, rollback for writes', 'commit is automatic'], correctIndex: 1, explanation: 'commit() = make changes permanent. rollback() = undo everything back to the last commit or transaction start. Requires setAutoCommit(false).' },
      { id: 'jat2-q17', question: 'What happens if an exception occurs AFTER debit but BEFORE credit in a bank transfer?', options: ['Only debit is saved — money disappears', 'rollback() in catch undoes everything — no money lost', 'MySQL fixes it automatically', 'The program crashes'], correctIndex: 1, explanation: 'Transaction with try-catch: catch block calls rollback() → undoes the debit. Both operations are reverted. Money is safe.' },
      { id: 'jat2-q18', question: 'What does Connection.setAutoCommit(true) do after a transaction?', options: ['Commits the transaction', 'RESTORES default auto-commit behavior', 'Rolls back the transaction', 'Closes the connection'], correctIndex: 1, explanation: 'Always restore auto-commit to true after transaction. Otherwise, future statements will NOT auto-commit and appear to "disappear" on connection close.' },
      { id: 'jat2-q19', question: 'Which is NOT a valid PreparedStatement setXxx() method?', options: ['setString()', 'setInt()', 'setBoolean()', 'setList()'], correctIndex: 3, explanation: 'There is no setList(). Use setObject() or convert list to array/JSON. Valid: setString, setInt, setLong, setDouble, setFloat, setBoolean, setDate, setTimestamp, setNull, setObject.' },
      { id: 'jat2-q20', question: 'What does it mean when we say PreparedStatement is "pre-compiled"?', options: ['It compiles Java code', 'The SQL is parsed and optimized ONCE by the database, then reused with different parameters', 'It runs before compilation', 'It is written in C'], correctIndex: 1, explanation: 'Database parses, compiles, and optimizes the SQL once. Subsequent executions just bind new parameters and execute — skipping the parsing/optimization overhead.' },
    ] }],
  },

  {
    id: 'java-adv-test-3', title: 'Test 3: Transactions & Batch Processing', category: 'Advanced Java',
    description: 'ACID in JDBC, commit/rollback, batch processing, connection lifecycle. 20 questions.',
    blocks: [{ type: 'quiz', id: 'ja-test-3-quiz', title: 'Transactions & Batch Processing (Days 5-6)', questions: [
      { id: 'jat3-q1', question: 'What does ACID stand for?', options: ['Atomicity, Consistency, Isolation, Durability', 'Access, Control, Integrity, Data', 'Automated, Consistent, Integrated, Distributed', 'Atomic, Concurrent, Indexed, Durable'], correctIndex: 0, explanation: 'ACID = Atomicity, Consistency, Isolation, Durability. Properties that guarantee reliable database transactions.' },
      { id: 'jat3-q2', question: 'What does Atomicity guarantee?', options: ['Transactions are fast', 'All-or-nothing — either ALL changes happen or NONE', 'Data is encrypted', 'Only one transaction runs at a time'], correctIndex: 1, explanation: 'Atomicity = transaction is indivisible. If any part fails, the ENTIRE transaction is rolled back. Debit+credit in transfer: both or neither.' },
      { id: 'jat3-q3', question: 'Where MUST rollback() be called in Java code?', options: ['In the try block', 'In the catch block (on exception)', 'After commit()', 'In the main method'], correctIndex: 1, explanation: 'rollback() in catch block — only called when an exception occurs. commit() in try block — only called when everything succeeds.' },
      { id: 'jat3-q4', question: 'What happens if you forget to call conn.setAutoCommit(true) after a transaction?', options: ['Nothing', 'Future SQL statements will NOT auto-commit — they appear to "vanish" when connection closes', 'The connection breaks', 'An error is thrown immediately'], correctIndex: 1, explanation: 'Auto-commit stays OFF. Future statements accumulate in a transaction that never commits. When connection closes without commit → all changes lost.' },
      { id: 'jat3-q5', question: 'When should you use transactions?', options: ['For every query', 'When MULTIPLE statements form ONE logical operation (transfer, booking, order placement)', 'Only for SELECT queries', 'Transactions are optional'], correctIndex: 1, explanation: 'Use when multiple SQL statements must succeed or fail TOGETHER. Bank transfers, inventory updates, order processing. Single queries do not need transactions.' },
      { id: 'jat3-q6', question: 'What is a savepoint in JDBC?', options: ['A database backup', 'A marker within a transaction — partial rollback to this point', 'The commit point', 'The rollback point'], correctIndex: 1, explanation: 'Savepoint sp = conn.setSavepoint("sp1"); ... conn.rollback(sp); Rolls back to savepoint (not the entire transaction). Useful for complex multi-step operations.' },
      { id: 'jat3-q7', question: 'What is the difference between batch and non-batch inserts?', options: ['Batch is slower', 'Batch sends ALL statements in ONE network call — much faster for bulk inserts', 'Batch is for SELECT only', 'Batch requires a different Connection type'], correctIndex: 1, explanation: 'Each executeUpdate() = one network round-trip. Batch: addBatch() for all rows, then ONE executeBatch(). Network overhead dramatically reduced.' },
      { id: 'jat3-q8', question: 'What is the purpose of .addBatch() before .executeBatch()?', options: ['Validates SQL syntax', 'Collects statements locally (in memory). executeBatch() sends them all at once.', 'Creates a backup', 'Clears previous batch'], correctIndex: 1, explanation: 'addBatch() adds the current parameter set to the batch queue (client-side, no network). executeBatch() sends ALL queued statements to the database.' },
      { id: 'jat3-q9', question: 'Can you use batch processing with SELECT queries?', options: ['Yes', 'No — batch is for INSERT/UPDATE/DELETE (modifications) only', 'Only with PreparedStatement', 'Only with Statement'], correctIndex: 1, explanation: 'executeBatch() returns int[] (update counts). SELECT returns ResultSets — cannot batch. Batch = for DML (INSERT/UPDATE/DELETE) only.' },
      { id: 'jat3-q10', question: 'What is a resource leak in JDBC?', options: ['Memory leak in the app', 'Not closing Connection/Statement/ResultSet — database resources remain allocated', 'Too many queries', 'Slow network'], correctIndex: 1, explanation: 'Unclosed resources = database keeps connections open, cursors allocated, locks held. Leads to max_connections error. Always close in finally or try-with-resources.' },
      { id: 'jat3-q11', question: 'What does MySQL prompt \'mysql>\' mean?', options: ['Error state', 'Ready for a new query', 'Multi-line query', 'Connection lost'], correctIndex: 1, explanation: 'mysql> = ready for new query. -> = multi-line continuation. \'> = unclosed single quote. "> = unclosed double quote.' },
      { id: 'jat3-q12', question: 'How do you cancel a broken multi-line query in MySQL CLI?', options: ['Exit the terminal', 'Type \\c and press Enter', 'Type ; and Enter', 'Restart MySQL'], correctIndex: 1, explanation: '\\c (backslash + c) cancels the current query and returns to mysql> prompt. Works from any prompt state (->, \'>, ">).' },
      { id: 'jat3-q13', question: 'What is the CE-3 worth in marks?', options: ['20', '25', '30', '35'], correctIndex: 1, explanation: 'CE-3 = 25 marks. 5 MCQs (5) + 2 coding at 5 marks (10) + 1 coding at 10 marks (10). Covers transactions, multithreading, batch processing.' },
      { id: 'jat3-q14', question: 'What does Connection.close() do EXACTLY?', options: ['Deletes the connection object', 'Releases the TCP connection and DATABASE resources', 'Commits the transaction', 'Rolls back the transaction'], correctIndex: 1, explanation: 'close() releases database and JDBC resources immediately. Uncommitted changes may be rolled back (vendor-specific). Always commit or rollback BEFORE closing.' },
      { id: 'jat3-q15', question: 'What is the proper order to close JDBC resources?', options: ['Connection first', 'ResultSet → Statement → Connection (reverse creation order)', 'Order does not matter', 'Statement first'], correctIndex: 1, explanation: 'Close in reverse creation order. RS depends on Statement, Statement depends on Connection. Closing Connection also closes Statement/RS (but do not rely on this).' },
      { id: 'jat3-q16', question: 'What happens if a transaction times out?', options: ['It commits automatically', 'It is ROLLED BACK', 'It keeps running', 'The connection closes'], correctIndex: 1, explanation: 'If a transaction exceeds the timeout (setQueryTimeout), it is rolled back. Set timeout: stmt.setQueryTimeout(seconds). Default is 0 (no timeout).' },
      { id: 'jat3-q17', question: 'What is the Isolation property in ACID?', options: ['Data is encrypted', 'Concurrent transactions do NOT interfere — each sees a consistent snapshot', 'Only one user at a time', 'Data is backed up'], correctIndex: 1, explanation: 'Isolation = transactions appear to run sequentially, even when concurrent. Database uses locking/MVCC to achieve this. Isolation levels: READ_UNCOMMITTED, READ_COMMITTED, REPEATABLE_READ, SERIALIZABLE.' },
      { id: 'jat3-q18', question: 'What is Connection.setTransactionIsolation() used for?', options: ['Setting user permissions', 'Controlling the ISOLATION LEVEL of transactions', 'Setting the database name', 'Setting the port'], correctIndex: 1, explanation: 'setTransactionIsolation(Connection.TRANSACTION_READ_COMMITTED). Higher isolation = more consistency but lower concurrency. Trade-off.' },
      { id: 'jat3-q19', question: 'What happens to auto-commit after conn.close() and reconnecting?', options: ['It stays OFF', 'It RESETS to default (true) for the new connection', 'It inherits the previous setting', 'It is random'], correctIndex: 1, explanation: 'Each new Connection starts with auto-commit = true (default). Previous connection\'s settings do not carry over.' },
      { id: 'jat3-q20', question: 'How do you verify the number of rows affected by a batch operation?', options: ['Count them manually', 'int[] results = ps.executeBatch() — each element is the row count', 'Use ResultSet.rowCount()', 'Check Connection.getUpdateCount()'], correctIndex: 1, explanation: 'executeBatch() returns int[]. results.length = number of batch entries. Each int = rows affected for that entry (or SUCCESS_NO_INFO = -2).' },
    ] }],
  },

  {
    id: 'java-adv-test-4', title: 'Test 4: Multithreading & Concurrency', category: 'Advanced Java',
    description: 'Thread creation, lifecycle, synchronization, ExecutorService. 20 questions.',
    blocks: [{ type: 'quiz', id: 'ja-test-4-quiz', title: 'Multithreading & Concurrency (Days 7-8)', questions: [
      { id: 'jat4-q1', question: 'What is the difference between start() and run()?', options: ['No difference', 'start() creates NEW thread + calls run(). run() just runs on CURRENT thread.', 'run() is deprecated', 'start() is for Runnable only'], correctIndex: 1, explanation: 'start() = new native OS thread, then JVM calls run(). run() directly = just a method call on the calling thread. No concurrency.' },
      { id: 'jat4-q2', question: 'Which is the PREFERRED way to create a thread?', options: ['Extend Thread', 'Implement Runnable', 'Use Callable directly', 'Use new Thread() without task'], correctIndex: 1, explanation: 'Runnable = task (composition). Extending Thread prevents extending anything else. Runnable works with ExecutorService. Use lambda: new Thread(() -> task).' },
      { id: 'jat4-q3', question: 'What is a race condition?', options: ['Two threads racing', 'Multiple threads accessing shared data unSYNCED — unpredictable results', 'A thread priority issue', 'A fast thread'], correctIndex: 1, explanation: 'Race condition = outcome depends on thread timing/scheduling. Without synchronization, interleaved reads/writes cause lost updates, corrupted data.' },
      { id: 'jat4-q4', question: 'What does synchronized guarantee?', options: ['Faster execution', 'Only ONE thread can execute synchronized block/method on same object at a time', 'All threads run synchronized', 'Thread ordering'], correctIndex: 1, explanation: 'synchronized = mutual exclusion. Threads acquire the object\'s monitor lock. Other threads BLOCK until the lock is released.' },
      { id: 'jat4-q5', question: 'What does volatile guarantee?', options: ['Atomicity', 'VISIBILITY — changes are seen by all threads immediately', 'Mutual exclusion', 'Transaction safety'], correctIndex: 1, explanation: 'volatile = visibility. Reads always from main memory (not CPU cache). Does NOT provide atomicity. count++ with volatile still has race conditions.' },
      { id: 'jat4-q6', question: 'Can volatile replace synchronized for count++?', options: ['Yes', 'NO — volatile does not make count++ atomic (three operations)', 'Only for small numbers', 'Only on 64-bit JVMs'], correctIndex: 1, explanation: 'count++ = read + increment + write (3 ops). Volatile only guarantees each read/write is visible, but another thread can interleave between them.' },
      { id: 'jat4-q7', question: 'What does wait() do that sleep() does NOT?', options: ['Pauses longer', 'RELEASES the lock — allows other synchronized methods to run', 'Uses less CPU', 'Works without synchronized'], correctIndex: 1, explanation: 'wait() releases the object\'s monitor lock and waits. sleep() KEEPS the lock. wait() must be called from synchronized block. wait() is woken by notify().' },
      { id: 'jat4-q8', question: 'Why should wait() be called in a while() loop, not if()?', options: ['Performance', 'Spurious wakeups — thread may wake without notify(). while() rechecks condition.', 'while() is faster', 'if() does not compile'], correctIndex: 1, explanation: 'After wakeup, condition may still be false. while() rechecks. if() proceeds with potentially invalid state. "Always invoke wait inside a loop" — Java documentation.' },
      { id: 'jat4-q9', question: 'What is ExecutorService?', options: ['A thread monitoring tool', 'A THREAD POOL manager — submit tasks, reuse threads, control concurrency', 'A debugger', 'A timer'], correctIndex: 1, explanation: 'ExecutorService = higher-level thread management. Thread pool, task submission (execute/submit), Future results, graceful shutdown. Preferred over manual thread creation.' },
      { id: 'jat4-q10', question: 'What is a daemon thread?', options: ['A high-priority thread', 'A background thread — JVM exits when ONLY daemon threads remain', 'A main thread', 'A debug thread'], correctIndex: 1, explanation: 'Daemon threads do NOT prevent JVM shutdown. Use for background tasks (logging, monitoring). Set with setDaemon(true) BEFORE start().' },
      { id: 'jat4-q11', question: 'What does thread.join() do?', options: ['Merges threads', 'CALLING thread WAITS until target thread TERMINATES', 'Stops the target thread', 'Starts the target thread'], correctIndex: 1, explanation: 'join() = wait for thread death. Main thread calls t.join() → main pauses until t finishes. Essential for sequencing: "finish download, then process."' },
      { id: 'jat4-q12', question: 'What is a thread pool?', options: ['A collection of thread objects', 'A group of PRE-CREATED reusable threads — avoids creation/destruction overhead', 'A thread-safe list', 'A synchronized block'], correctIndex: 1, explanation: 'Thread pool = fixed number of worker threads. Tasks are queued. Threads process tasks and go back to pool. Limits concurrency, reuses threads.' },
      { id: 'jat4-q13', question: 'How do you submit a task to ExecutorService?', options: ['pool.run(task)', 'pool.submit(task) or pool.execute(task)', 'pool.start(task)', 'pool.schedule(task)'], correctIndex: 1, explanation: 'submit() returns Future (can get result). execute() returns void (fire-and-forget). Both accept Runnable or Callable.' },
      { id: 'jat4-q14', question: 'What does pool.shutdown() do?', options: ['Kills all running tasks immediately', 'Stops accepting NEW tasks. Finishes running tasks then stops.', 'Restarts the pool', 'Pauses all tasks'], correctIndex: 1, explanation: 'shutdown() = graceful. No new tasks accepted. Running + queued tasks complete. shutdownNow() = attempts to stop all running tasks.' },
      { id: 'jat4-q15', question: 'What is the difference between Runnable and Callable?', options: ['No difference', 'Runnable returns void. Callable RETURNS a value (via Future).', 'Callable is deprecated', 'Runnable can throw checked exceptions'], correctIndex: 1, explanation: 'Runnable: void run(), cannot throw checked exceptions. Callable<V>: V call() throws Exception, returns value. Use with ExecutorService.submit() → Future<V>.' },
      { id: 'jat4-q16', question: 'What does Future.get() do?', options: ['Starts the task', 'BLOCKS until the task completes, then returns the result', 'Cancels the task', 'Checks task status'], correctIndex: 1, explanation: 'Future.get() = blocking call. Waits for Callable to finish, returns result. Throws ExecutionException if task threw. get(timeout, unit) with timeout.' },
      { id: 'jat4-q17', question: 'What thread state means "waiting to acquire a lock"?', options: ['RUNNABLE', 'BLOCKED', 'WAITING', 'TIMED_WAITING'], correctIndex: 1, explanation: 'BLOCKED = waiting for monitor lock (entering synchronized). WAITING = called wait(), join() without timeout. TIMED_WAITING = sleep(ms), wait(ms), join(ms).' },
      { id: 'jat4-q18', question: 'What does synchronized on a static method lock?', options: ['The instance (this)', 'The CLASS object (e.g., MyClass.class)', 'All instances', 'Nothing'], correctIndex: 1, explanation: 'static synchronized locks the Class object. All static synchronized methods on the same class share the SAME lock. Instance methods lock this (per-instance).' },
      { id: 'jat4-q19', question: 'What is deadlock?', options: ['A thread that dies', 'Two threads each hold a lock the other needs → NEITHER can proceed — forever stuck', 'A thread that runs too slowly', 'A lock that never releases'], correctIndex: 1, explanation: 'Deadlock: Thread A holds Lock1, needs Lock2. Thread B holds Lock2, needs Lock1. Both wait forever. Prevention: always acquire locks in same order.' },
      { id: 'jat4-q20', question: 'What is the key principle for avoiding deadlock?', options: ['Use more threads', 'Always acquire LOCKS in the SAME ORDER across all threads', 'Use volatile', 'Never use synchronized'], correctIndex: 1, explanation: 'Consistent lock ordering: if every thread acquires Lock1 then Lock2 (never Lock2 then Lock1), circular wait is impossible → no deadlock.' },
    ] }],
  },

  {
    id: 'java-adv-test-5', title: 'Test 5: Advanced Java Full Review', category: 'Advanced Java',
    description: 'Mixed questions from all 8 days. Comprehensive end-of-course assessment. 20 questions.',
    blocks: [{ type: 'quiz', id: 'ja-test-5-quiz', title: 'Advanced Java — Full Course Review', questions: [
      { id: 'jat5-q1', question: 'What is the full JDBC connection URL for a local MySQL database named "library_db"?', options: ['mysql://localhost/library_db', 'jdbc:mysql://localhost:3306/library_db', 'jdbc:mysql://library_db:3306', 'http://localhost:3306/library_db'], correctIndex: 1, explanation: 'Format: jdbc:mysql://host:port/database. Localhost = 127.0.0.1. Default MySQL port = 3306.' },
      { id: 'jat5-q2', question: 'Which JDBC statement type prevents SQL injection?', options: ['Statement', 'PreparedStatement', 'CallableStatement', 'All of them'], correctIndex: 1, explanation: 'PreparedStatement escapes parameters automatically. Statement concatenates strings — vulnerable. CallableStatement is for stored procedures.' },
      { id: 'jat5-q3', question: 'What does executeUpdate() return for a DELETE that removes 5 rows?', options: ['true', '5', '0', 'A ResultSet'], correctIndex: 1, explanation: 'executeUpdate() returns int = number of rows affected. DELETE 5 rows → returns 5. DDL (CREATE/DROP) → returns 0.' },
      { id: 'jat5-q4', question: 'What is the correct sequence to close JDBC resources?', options: ['Connection → Statement → ResultSet', 'ResultSet → Statement → Connection', 'Order does not matter', 'Statement → ResultSet → Connection'], correctIndex: 1, explanation: 'Close in REVERSE creation order. RS → Statement → Connection. Or use try-with-resources which handles ordering automatically.' },
      { id: 'jat5-q5', question: 'What transaction isolation level provides the highest consistency?', options: ['READ_UNCOMMITTED', 'READ_COMMITTED', 'REPEATABLE_READ', 'SERIALIZABLE'], correctIndex: 3, explanation: 'SERIALIZABLE = highest isolation. Transactions run as if sequential. Prevents dirty reads, non-repeatable reads, and phantom reads. Lowest concurrency.' },
      { id: 'jat5-q6', question: 'What is the purpose of addBatch() in JDBC?', options: ['Adds SQL to a queue', 'Collects parameter sets for later batch execution', 'Adds tables to a batch', 'Starts a transaction batch'], correctIndex: 1, explanation: 'addBatch() queues current parameters. executeBatch() sends all queued statements in ONE network call. Dramatically faster for bulk operations.' },
      { id: 'jat5-q7', question: 'Which method blocks until a thread completes?', options: ['sleep()', 'join()', 'wait()', 'yield()'], correctIndex: 1, explanation: 'join() = calling thread waits for target thread to TERMINATE. sleep() = self-pause. wait() = release lock and wait for notify. yield() = hint to scheduler.' },
      { id: 'jat5-q8', question: 'How do you create a thread pool of 4 threads?', options: ['new Thread(4)', 'Executors.newFixedThreadPool(4)', 'ThreadPool.create(4)', 'new ThreadPool(4)'], correctIndex: 1, explanation: 'Executors.newFixedThreadPool(4). Also: newCachedThreadPool(), newSingleThreadExecutor(), newScheduledThreadPool().' },
      { id: 'jat5-q9', question: 'What happens when you submit tasks to a fixed thread pool of 4 with 10 tasks?', options: ['Only 4 run — 6 are rejected', '4 run immediately — 6 queued. As threads finish, queued tasks execute.', 'All 10 run simultaneously', 'An error is thrown'], correctIndex: 1, explanation: 'Fixed pool: 4 active threads + unbounded queue. First 4 tasks run immediately. Remaining 6 wait in queue. As threads complete, next queued task starts.' },
      { id: 'jat5-q10', question: 'What is the difference between a checked and unchecked exception in JDBC?', options: ['No difference', 'SQLException is CHECKED — must be caught or declared. NullPointerException is UNCHECKED.', 'JDBC only throws unchecked', 'JDBC only throws checked'], correctIndex: 1, explanation: 'SQLException = checked (must handle). ClassNotFoundException = checked. Most others in JDBC are checked. NullPointerException from unboxing = unchecked.' },
      { id: 'jat5-q11', question: 'What does rs.absolute(5) do?', options: ['Moves to row 5 (if scrollable ResultSet)', 'Deletes row 5', 'Returns the 5th column', 'Validates 5 rows'], correctIndex: 1, explanation: 'absolute(n) moves cursor to row n (1-based). Requires scrollable ResultSet: createStatement(TYPE_SCROLL_INSENSITIVE, CONCUR_READ_ONLY).' },
      { id: 'jat5-q12', question: 'What is the purpose of JDBC\'s DatabaseMetaData?', options: ['Row data', 'Information ABOUT the database: tables, columns, supported features', 'Query optimization', 'Connection pooling'], correctIndex: 1, explanation: 'conn.getMetaData() → DatabaseMetaData. getTables(), getColumns(), supportsTransactions(), getDatabaseProductName().' },
      { id: 'jat5-q13', question: 'How is the Advanced Java mini-project evaluated?', options: ['Only final demo', 'Three phases: Proposal → Mid-Review → Final Demo + Viva', 'Only report', 'Only viva'], correctIndex: 1, explanation: '1. Proposal (idea + team + tech stack). 2. Mid-Review (working prototype). 3. Final Demo (complete app) + Individual Viva (explain any line).' },
      { id: 'jat5-q14', question: 'What does Thread.sleep() throw that you must handle?', options: ['SQLException', 'InterruptedException', 'IOException', 'RuntimeException'], correctIndex: 1, explanation: 'sleep() throws InterruptedException (checked). Must be in try-catch or declared with throws. InterruptedException signals "stop what you are doing."' },
      { id: 'jat5-q15', question: 'Which is true about synchronized?', options: ['Multiple threads can hold the same lock', 'Only ONE thread holds the lock. Others BLOCK.', 'It makes code faster', 'It prevents all errors'], correctIndex: 1, explanation: 'synchronized = mutual exclusion. One thread at a time per lock. Others block until lock released. Provides both atomicity AND visibility.' },
      { id: 'jat5-q16', question: 'What is the purpose of try-with-resources in JDBC?', options: ['Retry failed queries', 'AUTO-CLOSE Connection, Statement, ResultSet when try block exits', 'Execute queries faster', 'Create database connections'], correctIndex: 1, explanation: 'try (Connection c = ...; Statement s = ...) { ... }. Resources auto-close when block exits (even on exception). No finally block needed.' },
      { id: 'jat5-q17', question: 'What happens if auto-commit is OFF and you close the connection without commit or rollback?', options: ['Changes are auto-committed', 'Changes are ROLLED BACK (behavior varies by DB — MySQL rolls back)', 'Changes are saved', 'Error is thrown'], correctIndex: 1, explanation: 'Most databases ROLL BACK uncommitted changes on connection close. Do not rely on this — always explicitly commit or rollback before closing.' },
      { id: 'jat5-q18', question: 'What is notifyAll() vs notify()?', options: ['Same thing', 'notifyAll() wakes ALL waiting threads. notify() wakes ONE (random).', 'notify() is deprecated', 'notifyAll() is faster'], correctIndex: 1, explanation: 'notify() = one random thread woken. notifyAll() = all threads woken, compete for lock. Prefer notifyAll() unless you are sure only one thread needs waking.' },
      { id: 'jat5-q19', question: 'What is the project tech stack recommended in the course?', options: ['MongoDB + Express + React + Node', 'MySQL + JDBC + Java Swing', 'PostgreSQL + Hibernate + Spring', 'SQLite + Python'], correctIndex: 1, explanation: 'MySQL (XAMPP) → JDBC (java.sql) → Java Swing (GUI). mysql-connector-j.jar for driver. Maven/Gradle optional for dependency management.' },
      { id: 'jat5-q20', question: 'What Java package contains JDBC classes?', options: ['javax.jdbc', 'java.sql and javax.sql', 'com.mysql.jdbc', 'org.jdbc'], correctIndex: 1, explanation: 'java.sql = core JDBC (Connection, Statement, ResultSet). javax.sql = extended (DataSource, ConnectionPool). Driver classes are in vendor packages (com.mysql.cj.jdbc).' },
    ] }],
  },
];